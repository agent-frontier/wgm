# Docs Audit Report — 2026-07-04T1912Z — docs-audit-core

> Generated by the wgm docs-audit swarm (`wgm-docs-junior`, `wgm-docs-senior`, `wgm-docs-principal`,
> `wgm-docs-pm` → consolidated by `wgm-docs-writer`). This is the qualitative pass; structural checks
> (required files, links, fences, placeholders) live separately in `scripts/check-docs.sh` (GREEN at
> time of writing — 43 files, 32 mermaid diagrams, 11 agent files).

- **Scope:** `SKILL.md`, `README.md`, all of `docs/`, all of `references/`, all 11
  `.github/agents/*.agent.md` files, and the `assets/` templates touched by this change.
- **Track:** Full
- **Trigger:** Ship/Handoff — this run doubles as the feature's own demo-validation (this mechanism's
  first real, end-to-end dispatch against wgm's own docs, not a synthetic example).
- **Overall verdict:** AMBER — one real defect found and fixed during this run; two follow-ups
  queued as Agent actions; one genuine dissent preserved for the operator.

## Persona findings

### Junior developer — clarity & onboarding
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `SKILL.md` ("Backpressure is the skill") | Explains the concept narratively but doesn't lead with a crisp one-line definition — one already exists verbatim in `references/ralph-loop.md` ("a deterministic pass/fail signal: test, type-check, build, lint, HTTP probe"). A first-time reader of `SKILL.md` alone has to infer it from the elaboration. | AMBER | Open the section with that one-line definition before the elaboration. |
| `SKILL.md` (Triage step 4, Ralph-lite vs Ralph-full) | "small/medium" vs "large/ambiguous" has no concrete sizing threshold, unlike the Quick/Standard/Full track table just above it (which gives a concrete "1-5 file change"). A newcomer may not confidently self-classify. | GREEN | Optional: cross-reference the track table's sizing language from this bullet. |
| `docs/operator/playbook.md` (new, this run) | Reads well end-to-end for a first run; the Day-1 checklist front-loads the root-vs-`.wgm/` placement rule before it matters. | GREEN | none |

### Senior developer — correctness, completeness, maintainability
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `README.md` (Capabilities) | **Found and fixed during this audit run:** the "Two-stage subagent review" bullet still said "six role-specialized agents" after this same run added 5 more (11 total) — a real, self-contradicting stale count within one file (the Repository-layout block in the same README already said "eleven"). | RED (found), fixed in this run | Corrected to "eleven" and reworded in this run — see Agent actions. |
| `references/subagents.md` (new Docs-audit swarm section) | Correctly cross-references the existing dissent-preservation section instead of duplicating it; "Model selection" and "Cross-links" sections both updated consistently with the rest of the file. | GREEN | none |
| `scripts/check-docs.sh` (new check 7) | Globs `.github/agents/*.agent.md` generically rather than hardcoding the 5 new file names, so it regression-protects the original 6 agent files too. Verified: 11/11 pass. | GREEN | none |
| `docs/plans/2026-06-16_RALPH_LANDSCAPE.md` | Still says "six-role swarm" — now stale relative to the shipped count of eleven. | AMBER | See Dissent — Principal disagrees this needs a fix. |

### Principal developer — architecture, strategic fit, consistency
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `specs/CONSTITUTION.md`, `specs/docs-audit-core.md` (local, this build) | Conform to the constitution's own non-negotiables (the never-gitignore-downstream-by-default rule is stated explicitly and consistently in both). No undisclosed deviations. | GREEN | none |
| `docs/plans/2026-06-16_RALPH_LANDSCAPE.md` | Its "six-role swarm" mention is a point-in-time snapshot of a dated, archival plan doc — not a living reference. Archival plan docs under `docs/plans/` are, by this repo's own convention, not held to the same current-state freshness bar as `SKILL.md`/`references/`. | GREEN (by design) | See Dissent — Senior flagged this as AMBER. |
| `references/docs-audit.md` | Its placement/never-gitignore rule is consistent with `references/artifacts.md` and does not contradict `specs/CONSTITUTION.md`'s non-negotiables — cross-checked directly, no contradiction found. | GREEN | none |
| `SKILL.md` | Sits at 279 lines after this change (started at 267; project's own stated budget is ~500) — conforms to the constitution's lean-protocol principle. | GREEN | none |

### Project manager — status, risk, traceability
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `SKILL.md` frontmatter (`version: "0.3"`) | Verified via `git log -p -- SKILL.md`: version bumps (0.1→0.2→0.3) happen only at deliberate maintainer release-cuts, not per feature PR — several feature PRs landed between each bump. This PR should not bump it. | GREEN (confirmed, no action) | none — leave the version bump to the maintainer's release process. |
| `IMPLEMENTATION_PLAN.md` (local, gitignored per this repo's `.gitignore`) | Fully tracks this feature's tasks as done, but that record disappears once the local scratch is cleaned up. | AMBER | Agent action: write a dated `docs/plans/` summary before Ship so the decision record survives (queued next in this build). |
| `docs/README.md`, `README.md` | Both now correctly index `playbook.md` and `references/docs-audit.md` — a stakeholder can trace the new capability from the root without reading `SKILL.md` first. | GREEN | none |

## Consolidated report (wgm-docs-writer)

### README index check
Root `README.md` and `docs/README.md` both now index every new doc/agent file added this run
(`playbook.md`, `references/docs-audit.md`, the 5 new agent files via the `subagents.md` row). No
orphaned links found for this scope; `scripts/check-docs.sh` check 4 (link resolution) confirms
green. This run's scope was the docs-audit feature itself, not a full-repo index sweep.

### Agent actions
> The agent can execute these directly and deterministically — no human judgment required.

| # | Finding | Raised by | Doc(s) | Action |
|---|---|---|---|---|
| 1 | Stale "six role-specialized agents" count contradicting "eleven" in the same file | Senior | `README.md` | **Done in this run** — corrected to "eleven," bullet reworded for accuracy. |
| 2 | "Backpressure is the skill" doesn't lead with the one-line definition already written elsewhere | Junior | `SKILL.md` | **Done in this run** — section now opens with "Backpressure is a deterministic pass/fail signal…" before the elaboration. |
| 3 | This feature's decision record currently lives only in a gitignored local plan | PM | `IMPLEMENTATION_PLAN.md` (local) | Queued as the next task in this build: write a dated `docs/plans/` summary (e.g. `docs/plans/2026-07-04_DOCS_AUDIT_CORE_PLAN.md`). |

### Operator actions
> These need a human decision, external confirmation, or access the agent doesn't have.

| # | Finding | Raised by | Doc(s) | Why it needs a human |
|---|---|---|---|---|
| 1 | Whether `docs/plans/2026-06-16_RALPH_LANDSCAPE.md`'s stale "six-role swarm" mention should be corrected or intentionally left as a dated snapshot | Senior vs. Principal (see Dissent) | `docs/plans/2026-06-16_RALPH_LANDSCAPE.md` | A policy/scope judgment about whether archival plan docs are held to the current-state freshness bar — not a mechanical fix either way. |
| 2 | Confirm `SKILL.md`'s version should stay at "0.3" for this change (not bumped) | PM | `SKILL.md` | Release-cut timing is the maintainer's call; precedent supports not bumping, but the decision itself belongs to the operator. |

### Dissent
> Disagreements between personas, preserved rather than averaged away.

| Finding | Persona A view | Persona B view | Preserved as |
|---|---|---|---|
| `docs/plans/2026-06-16_RALPH_LANDSCAPE.md` says "six-role swarm," now stale vs. the shipped eleven | Senior: AMBER — factually incorrect now, should be corrected | Principal: GREEN — it's a dated archive documenting a past state by design; "fixing" it would blur the archive's point-in-time purpose | Operator action #1 above — a scope/policy call, not resolved by this report |

## Verdict history
- First entry for this project — no prior report to compare against.
