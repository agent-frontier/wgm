# Docs Audit Report — 2026-07-06T0602Z — hive-growth-loop-pr61

> Generated by the wgm docs-audit swarm (`wgm-docs-junior`, `wgm-docs-senior`, `wgm-docs-principal`,
> `wgm-docs-pm` → consolidated by `wgm-docs-writer`). This is the qualitative pass; structural checks
> (required files, links, fences, placeholders) live separately in `scripts/check-docs.sh`.

- **Scope:** PR #61 (`feat/hive-growth-loop`, commit `93dd1e3`) — the "Hive Growth Loop" feature: its
  own 12-file diff (`.github/agents/wgm-hermes.agent.md`, `SKILL.md`, `assets/wgm-hive.template.yml`,
  `docs/plans/2026-07-06_HIVE_GROWTH_LOOP.md`, `references/artifacts.md`,
  `references/issue-intake.md`, `references/self-improvement.md`, `references/subagents.md`,
  `scripts/harvest-hive.sh`, `scripts/swarm.sh`, `scripts/test-harvest-hive.sh`,
  `scripts/test-swarm.sh`) plus the wider surface the four personas checked it against for drift:
  `README.md`, `docs/README.md`, `SECURITY.md`, `CONTRIBUTING.md`, `.github/workflows/ci.yml`,
  `.github/PULL_REQUEST_TEMPLATE.md`, `docs/agent/lifecycle.md`, `docs/operator/playbook.md`,
  `docs/operator/running-the-loop.md`, `references/adr.md`, `docs/plans/2026-06-16_GROWTH_LOOP.md`.
- **Track:** Standard (not explicitly declared in the four source reports; the PR's scale — 12
  changed files, ~1,062 lines, a brand-new subagent role — would also plausibly support Full, but
  the mandatory Ship/Handoff trigger below applies either way).
- **Trigger:** Ship/Handoff — mandatory pass before PR #61 is declared shipped.
- **Overall verdict:** AMBER — most RED findings were caught and fixed in this same PR before this
  report was filed (see "Fixed in this PR" below), which is real, verified progress; but this pass
  still surfaces a genuine, unimplemented RED-severity gap deferred as an Operator decision (real
  per-lesson splitting), several more genuine Operator-level policy questions, and roughly two dozen
  still-open, purely mechanical Agent actions that were **not** part of this PR's pre-merge fix pass.
  Unlike the immediately preceding entry (GREEN — every finding closed before filing), this run does
  not close everything actionable, so it does not earn GREEN.

## Persona findings

### Junior developer — clarity & onboarding
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `README.md` (untouched by the original PR diff) | Still said "eleven role-specialized agents" (Capabilities bullet, repo-layout tree comment) while `SKILL.md` (edited by this PR) said "twelve"; the `references/`/`assets/`/`scripts/` tree-comment lines also didn't list `issue-intake`, `wgm-hive.template.yml`, or `harvest-hive.sh`. A newcomer reading only the README — `docs/README.md` itself calls it "the quickstart" — would never learn this feature exists, and would hit a flat contradiction against `SKILL.md`. Precedent: this exact omission class (tree-comment enumeration drifting from new files) was already raised and fixed once (`docs/audit/2026-07-05T0622Z_pr41-45-post-merge-audit.md`, finding #8) — a regression on an already-established discipline. | RED | Update the subagent count, Capabilities bullet, and all three tree-comment lines in `README.md` in this same PR. |
| `SKILL.md` (Phase 0, consent question) | "Hive Growth Loop" is named before it's defined, and the metaphor itself is never explained inline. | AMBER | Add a one-clause definition inline at first use, not purely by citation. |
| `references/subagents.md`, `references/self-improvement.md` | "Swarm" already had 3 meanings in this codebase (all subagents; the docs-audit swarm; `scripts/swarm.sh` fan-out); this PR adds 2 more overlapping insect/collective metaphors with no reconciliation — "hive," and "node" as an unexplained synonym for `swarm.sh`'s own consistently-used term "stream." | AMBER | Reuse "stream" instead of introducing "node," and add one short line mapping swarm/hive/stream. |
| `references/issue-intake.md` | Never mentions the Hive Growth Loop or `.github/wgm-hive.yml` — its role in this feature is invisible from the file itself. | AMBER | Add one sentence near the top noting it doubles as a Hive Growth Loop capture source. |
| `assets/wgm-hive.template.yml`, `scripts/harvest-hive.sh`, `scripts/swarm.sh` | `sources:` is documented as an actionable control, but is never read by any shipped code — verified `harvest-hive.sh` only parses `consent`/`auto_report`, and `swarm.sh` doesn't touch the consent file at all. Trimming `sources:` currently has zero observable effect. | RED | Wire it up, or mark it plainly as "not yet enforced," and clarify its scope vs. `issue-intake.md`. |
| `assets/wgm-hive.template.yml`, `SKILL.md`, `scripts/harvest-hive.sh` | Three different, non-matching wordings of "the one question wgm asks," plus an undocumented second code path (the script's own standalone ask, distinct from Triage asking once and writing the file). | AMBER | Designate one canonical wording and have the script + docs reuse it; add a line reconciling the two ask paths. |
| `assets/wgm-hive.template.yml` | The template exposes `consent:false` + `auto_report:true` as a possible combination, but no doc explains it's a silent no-op. | AMBER | State explicitly that both fields must be `true` together for automatic filing. |
| `references/artifacts.md` | "The hive consent/levers file" — orphan term "levers" never defined. | GREEN | — |
| `references/self-improvement.md`, `references/subagents.md`, `.github/agents/wgm-hermes.agent.md` | The "researched Hermes-style multi-agent pattern" is invoked three times but never named/linked, breaking the repo's own external-research citation rule. | AMBER | Name/link the source per the existing convention, or soften to "inspired by the messenger-god framing" without implying an uncited precedent. |
| `references/self-improvement.md` | The file's own "flywheel" summary line wasn't updated for the new "Swarm harvest" section. | AMBER | Add "swarm harvest" to the one-line flywheel summary. |
| `references/self-improvement.md` | No worked example for the two most novel mechanics — anonymization, and the consolidated swarm-memories file. | AMBER | Add a worked example for each. |
| `docs/operator/running-the-loop.md`, `docs/operator/playbook.md` | Operator docs that already walk through the exact affected commands weren't updated (Swarm section; playbook checklist). | AMBER | Add a short note to both. |
| `SKILL.md` (References section) | The file's own end-of-file artifact inventory omits the two new files this PR introduces (`wgm-hive.template.yml`, `harvest-hive.sh`). | GREEN | Add both to the References bullets. |

### Senior developer — correctness, completeness, maintainability
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `.github/agents/wgm-hermes.agent.md`, `references/subagents.md`, `scripts/harvest-hive.sh` | Docs state Hermes "does not ask the question itself," but `harvest-hive.sh`'s `load_consent_state`/`prompt_for_consent` prints the question itself when the consent file is absent; if stdin isn't a TTY it silently defaults to declined **and permanently writes `consent: false`** to the committed file, with no human ever seeing/answering it. | RED | Document the fallback explicitly and reconcile with "Hermes does not ask," or change the script so an absent file is left unwritten (defer entirely to Triage) rather than auto-declined-and-persisted. |
| `scripts/harvest-hive.sh` (`anonymize_content`) | Anonymization under-scrubs exactly the categories the docs promise: the repo/org regex is lowercase-only (`MyOrg/SecretRepo` passes through unredacted); bare hostnames without `http(s)://` are never touched despite "hostnames" being explicitly listed; Windows-style paths (`C:\Users\...`) are untouched. | RED | Narrow the doc's claim to actual scope, or fix the regexes (case-insensitive matching, bare-hostname detection, Windows paths). |
| `scripts/harvest-hive.sh` | "One lesson per report" isn't implemented — no lesson-selection logic; anonymizes and files the entire raw file as one issue, titled from only the first line. Verified against a realistic multi-entry, swarm-tagged fixture: 5 unrelated entries across 2 swarm streams became one issue titled after entry #1. | RED | Document the whole-file-dump behavior if intentional, or add real per-lesson extraction and a multi-entry test fixture. |
| `references/self-improvement.md`, `references/subagents.md`, `scripts/swarm.sh` | "Two dispatch points" (standing report after every swarm) is asserted three times, implemented nowhere, and contradicted by neighboring prose in the same file — `swarm.sh` only ever writes `.wgm/memories.md`; it never touches `.github/wgm-hive.yml`, `gh`, or anonymization. | RED | Pick one model consistently: wire an actual post-swarm Hermes dispatch into `SKILL.md`/`swarm.sh`, or drop "two dispatch points" and state plainly that swarm only feeds the next Ship/Handoff harvest. |
| `README.md`, `docs/README.md` | Weren't updated for the 12th role or `references/issue-intake.md` — reproducing a bug class this repo already found and fixed once (`docs/audit/2026-07-04T1912Z_docs-audit-core.md`). | AMBER | Update role count and add the missing Map row. |
| `README.md` | "Self-improvement flywheel" bullet still describes the pre-PR opt-in-only model. | AMBER | Rewrite to describe the Hive Growth Loop's consent-once/automatic model. |
| `docs/agent/lifecycle.md` | Phase table wasn't updated (Triage row omits the new consent check; Ship row unchanged). | AMBER | Update both rows. |
| `docs/operator/running-the-loop.md` | Swarm section never mentions the new memory-consolidation behavior. | AMBER | Add a note. |
| `SKILL.md` (References section) | Own References list never names `scripts/harvest-hive.sh`, unlike every other primary script. | AMBER | Add it. |
| `SKILL.md` (Phase 0) | The mandatory first-question consent check isn't scoped away from read-only modes (`/wgm review`, `/wgm analyze only`). | AMBER | Scope the check, or state explicitly that it is intentionally unconditional. |
| `SKILL.md`, `references/self-improvement.md`, `references/artifacts.md`, `assets/wgm-hive.template.yml`, `.github/agents/wgm-hermes.agent.md`, `scripts/harvest-hive.sh` | The consent-trigger rule is restated near-verbatim in ≥5 places with already-detectable drift; the "four sources" catalog is independently duplicated in prose + template + a hardcoded script heredoc with no single source of truth. | AMBER | Designate one canonical source per catalog; have the rest cite it. |
| `references/subagents.md` | Hive-courier bullet omits the "not a redaction guarantee" caveat mandated elsewhere. | AMBER | Add it (already present verbatim in `wgm-hermes.agent.md`). |
| `references/issue-intake.md` | Doesn't cross-link back to `wgm-hermes`/`subagents.md`. | AMBER | Add the cross-link. |
| `references/self-improvement.md` ("Consent & continuous mode") | `consent: true, auto_report: false` (intentional, correctly gated in code) is never walked through — only absent/true+true/false are covered. | AMBER | Add the missing bullet. |
| `assets/wgm-hive.template.yml` | Quoted "Question wgm asked" doesn't match the actual runtime prompt string verbatim. | GREEN | Reconcile wording (same fix as the canonical-wording finding above). |
| `docs/audit/2026-07-04T1912Z_docs-audit-core.md` | Cites an old step number pre-renumbering. | GREEN | None — expected; archives are point-in-time by established precedent. |

### Principal developer — architecture, strategic fit, consistency
> Note: this repo has no `specs/CONSTITUTION.md` of its own; "constitution conformance" judged
> against wgm's own protocol docs.

| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `SKILL.md` vs. `references/self-improvement.md` vs. `scripts/harvest-hive.sh` | Harvest's scan scope drifts across the PR's own two canonical docs, and neither matches the implementation. `SKILL.md`'s Ship/Handoff bullet says to scan memories, swarm-consolidated node lessons, **and this project's own GitHub Issues** — but `self-improvement.md`'s own "Harvest" section is untouched and still says only "scan `.wgm/memories.md`." Verified `harvest-hive.sh` only ever reads a memories file; it never reads the host project's own GitHub issue bodies. This is exactly the drift this PR's own new Cross-pollinate rule ("sync the summary docs in the same PR") exists to prevent, and it didn't catch itself. | RED | Correct `SKILL.md`'s bullet to match the narrower, actually-implemented scope, or wire up real issue-body scanning with its own anonymization treatment. |
| `references/self-improvement.md` ("What this is not") | "Not telemetry by default" contradicts the behavior it describes — the heading asserts non-telemetry; the body concedes automatic, recurring, unprompted, no-further-prompting transmission once consented, which is what most engineers call telemetry, just a responsibly-gated kind. | AMBER | Rename to "No telemetry without consent, ever" or similar; let the body own that consented reporting is genuinely automatic. |
| `references/adr.md` | The design doc's ADR-placement callout isn't recorded in `adr.md` itself, unlike the analogous docs/audit gitignore exception, which IS recorded in `artifacts.md`. A future contributor consulting `adr.md` directly wouldn't find this carve-out. | AMBER | Add a short bullet to `adr.md` mirroring the docs/audit carve-out precedent. |
| `SKILL.md` (Phase 0), `references/stall-recovery.md` | The mandatory interactive consent-ask isn't reconciled with the protocol's own preferred non-interactive (Ralph-full, headless coding-agent) execution mode. No doc says what happens when Triage step 2 is reached with no human present; `stall-recovery.md`'s existing "unattended → preserve, revert, hand off" idiom isn't referenced. | AMBER | State the non-interactive fallback explicitly, mirroring `stall-recovery.md`'s existing convention. |
| `references/self-improvement.md` (Health check) | The "3 self-referential PRs in a row" guardrail rule doesn't obviously count this PR's own kind of work — a large internal feature-engineering PR isn't named as either a "docs-audit pass" or a "Cross-pollinate PR," though it's arguably more self-referential than either. | AMBER | Broaden the rule's scope, or have this PR acknowledge it counts. |
| `references/self-improvement.md` (Capture vs. Swarm harvest) | Token-budget math isn't reconciled — Capture claims the new sources "don't change how little of it wgm keeps around at once," but Swarm harvest unconditionally concatenates every stream's full memories file with no trim step, against the ~2000-token budget stated elsewhere. | AMBER | State explicitly when the trim rule fires relative to swarm consolidation, or soften the Capture claim. |
| `references/artifacts.md` | "The one exception" is claimed twice, for two different rules (placement vs. git-tracking) — brittle/easy to conflate, and doesn't acknowledge `.wgm/memories.md` already deviates from greenfield-root the same way. | GREEN/AMBER | Disambiguate the two claims; acknowledge the `memories.md` parallel. |
| `references/artifacts.md` | No carve-out for the degenerate case where the host project IS `agent-frontier/wgm` itself (the consent question being asked inside wgm's own repo, dogfooding on itself) — other artifacts in the same file special-case this; this one doesn't. | GREEN | Add the carve-out note. |
| `references/subagents.md` | Checked and sound: `wgm-hermes`'s "never opens/merges a PR" boundary — verified consistent across every citing file. | GREEN | None — noted as a strength. |

### Project manager — status, risk, traceability
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `IMPLEMENTATION_PLAN.md` / backpressure checklist (this session) | Verified accurate — independently re-ran all 9 items, every number matches. | GREEN | None. |
| `README.md`, `docs/README.md` | Stale "eleven" subagent count (2 files, 4 locations) — same regression class this repo's own docs-audit history already caught and fixed once. | AMBER | Update both files. |
| PR #61 description ("Notes for reviewers") | Slightly overstates "nothing changes for existing installs" — every existing project's next `/wgm` run will see a brand-new mandatory question and get a new file committed, regardless of answer. | AMBER | Correct the PR description. |
| `SECURITY.md` ("Safety model") | Untouched — no mention of the new capability. This PR adds a materially new external side effect (auto-filing anonymized-but-not-guaranteed-clean content to a public repo), and the one doc whose entire job is this kind of disclosure doesn't mention it. | RED | Add a disclosure bullet naming the capability, its consent gate, and the anonymization caveat. |
| `docs/agent/lifecycle.md`, `docs/operator/playbook.md`, `README.md` (flywheel bullet) | None of the "quick skim" surfaces mention the new consent question or automatic reporting; only "deep" docs were touched. | AMBER | Add notes to all three. |
| `docs/plans/2026-07-06_HIVE_GROWTH_LOOP.md` | Linked from only one file (`self-improvement.md`); `artifacts.md` and `subagents.md` describe the feature but never link back to "why." | AMBER | Add cross-links. |
| `references/adr.md` | Doesn't record the now-twice-used `docs/plans/` precedent for wgm's own protocol ADRs. | AMBER | Add the precedent note. |
| `references/self-improvement.md`, `references/subagents.md`, `.github/agents/wgm-hermes.agent.md` | Unsourced "researched Hermes-style pattern" and ".agent/.ai research" claims — no citation, breaking the repo's own citation convention. | AMBER | Name/link the source, or soften the claim. |
| `docs/audit/README.md`, `.github/PULL_REQUEST_TEMPLATE.md` | `docs/audit/README.md` doesn't need a generic PR entry (confirmed correct per its own scope) — but this PR's own backpressure checklist has no docs-audit line item stating "ran it" or "deferred." | AMBER | Add a checklist line item to the PR template. |
| `assets/wgm-hive.template.yml` | `sources:` field appears unenforced by any script — unclear who's responsible for enforcing it. | AMBER | Mark plainly as not-yet-enforced, or assign ownership. |
| `scripts/harvest-hive.sh` (`find_duplicate_issue`) | Issue-dedup heuristic has no stated limitation/caveat, unlike anonymization which repeatedly gets one. | AMBER | Add a caveat sentence. |
| `scripts/harvest-hive.sh` (`anonymize_content`) | **Verified to over-redact benign content** — ran it directly against ordinary prose citing public repos; a fraction and two real public repo names (`golang/go`, `3/4`, `facebook/react`) were all wrongly redacted. Docs only disclose under-redaction risk, never over-redaction. | AMBER (verified) | Tune the pattern; disclose the over-redaction risk too. |
| `SKILL.md` (Phase 0), `scripts/swarm.sh` | Undefined interaction between the new mandatory first-run consent question and `swarm.sh`'s parallel worktrees — N parallel non-interactive streams could each independently hit "ask the question" with nobody present; no documented fallback, no test coverage. | AMBER | Document and test the parallel-worktree case. |
| `.github/workflows/ci.yml`, `CONTRIBUTING.md` | CI does not run the new `scripts/test-harvest-hive.sh`, and `CONTRIBUTING.md`'s canonical backpressure list wasn't updated — the script has zero ongoing enforcement going forward. | RED | Add the CI step and the CONTRIBUTING.md line. |

## Consolidated report (wgm-docs-writer)

### README index check
`docs/README.md`'s Map already correctly lists `references/issue-intake.md` (post-fix, see Agent
action #1) and, after the in-progress `README.md` fix lands, the repo-layout tree comment correctly
names `wgm-hive.template.yml` and `harvest-hive.sh`. `.github/agents/*.agent.md` files (including
the new `wgm-hermes.agent.md`) and `assets/`/`scripts/` entries are consistent with this repo's
existing convention of not being individually indexed as Map rows (they're named in prose bullets
instead) — no gap there.

One genuine gap **not** named verbatim by any of the four personas, but surfaced by this
README-index-check step itself (`references/docs-audit.md`'s consolidation algorithm, step 4 —
"flag any doc that exists but isn't indexed anywhere"): `docs/README.md`'s **"Plans & roadmap"**
list indexes every prior `docs/plans/*.md` file — including this feature's own predecessor,
`2026-06-16_GROWTH_LOOP.md` — but omits the newest one, `docs/plans/2026-07-06_HIVE_GROWTH_LOOP.md`,
entirely. This compounds PM's related finding (below) that the design doc is under-linked from other
reference docs — see Agent action #17, which folds both angles into one fix.

No other orphaned links or missing-file references were found in `README.md` / `docs/README.md`
during this pass (scoped to the docs this feature touches or should touch, not an exhaustive
whole-repo link sweep).

### Agent actions
> The agent can execute these directly and deterministically — no human judgment required.
> Per the implementing agent's note, eight cross-corroborated findings below are already being fixed
> in this same PR before merge. Six were directly visible in the working tree and diffed at
> consolidation time; two (`swarm.sh`'s post-swarm dispatch, and the `SKILL.md`/`self-improvement.md`
> wording corrections) were not yet visible but are represented as landing in this same PR before
> merge — each row states which applies, so the paper trail doesn't overstate what was directly
> observed versus represented.

**Fixed in this PR:**

| # | Finding | Raised by | Doc(s) | Action |
|---|---|---|---|---|
| 1 | Stale "eleven→twelve" subagent count and missing tree-comment entries (`issue-intake`, `wgm-hive.template.yml`, `harvest-hive.sh`); `README.md`'s flywheel bullet still described the pre-PR opt-in-only model; `docs/README.md`'s Map was missing the `references/issue-intake.md` row | Junior (RED), Senior (AMBER — count; AMBER — flywheel bullet), PM (AMBER) — **severity dissent, see Dissent #1** | `README.md`, `docs/README.md` | **Fixed — verified via working-tree diff.** Role count and tree comments updated in `README.md`; flywheel bullet rewritten to describe the Hive Growth Loop's consent-once/automatic model; `docs/README.md`'s role count updated and its Map gained the `references/issue-intake.md` row. |
| 2 | CI never ran `scripts/test-harvest-hive.sh`; `CONTRIBUTING.md`'s canonical backpressure list omitted it | PM (RED) | `.github/workflows/ci.yml`, `CONTRIBUTING.md` | **Fixed — verified via working-tree diff.** Added a "Harvest-hive harness (bash)" step to `ci.yml` and a `bash scripts/test-harvest-hive.sh` line to `CONTRIBUTING.md`'s backpressure suite. |
| 3 | `SECURITY.md`'s "Safety model" section never disclosed the new automatic-publish capability | PM (RED) | `SECURITY.md` | **Fixed — verified via working-tree diff.** Added a third bullet naming `scripts/harvest-hive.sh`/`wgm-hermes` as the one capability that can send data to a public third-party repo, its off-by-default consent gate, the anonymize-first/not-a-redaction-guarantee caveat, and its no-PR-authority boundary. |
| 4 | Anonymization under-scrubbed mixed-case repo/org names, bare hostnames, and Windows-style paths | Senior (RED) | `scripts/harvest-hive.sh` | **Fixed — verified via working-tree diff.** Matching is now case-insensitive (the `I` flag) on the URL/email/path/repo-org/self-repo patterns; added a Windows-path pattern and a bare-hostname pattern for common internal-style suffixes (`.internal`/`.corp`/`.local`/`.lan`). **Residual, not silently folded in as fully closed:** hostname coverage is still partial — an arbitrary hostname without `http(s)://` and without one of those four suffixes still passes through unredacted. |
| 5 | "Two dispatch points" (standing after every swarm + at Ship/Handoff) was asserted in `self-improvement.md` and `subagents.md`, but `scripts/swarm.sh` never dispatched Hermes/`harvest-hive.sh` at all | Senior (RED) | `scripts/swarm.sh`, `references/self-improvement.md`, `references/subagents.md` | **Fixed — represented by the implementing agent as landing in this PR; not yet visible in the working tree at consolidation time.** Wires an actual post-swarm dispatch of `harvest-hive.sh`/`wgm-hermes` into `scripts/swarm.sh`, closing the "asserted but not implemented" gap. |
| 6 | `harvest-hive.sh`'s non-interactive consent fallback silently declined **and permanently persisted `consent: false`** to the committed file with no human ever answering | Senior (RED — this half of the finding) | `scripts/harvest-hive.sh` | **Fixed — verified via working-tree diff.** The non-interactive (`[[ ! -t 0 ]]`) branch no longer calls `write_or_preview_consent`; it now leaves `.github/wgm-hive.yml` unwritten so a later interactive Triage session can still ask a real human. **Residual, not fully closed:** the *interactive* branch of `prompt_for_consent` (stdin is a TTY, file absent) still asks the question directly from inside the script — this does not fully resolve the doc claim that "Hermes does not ask the question itself"; see Operator action #3. |
| 7 | `SKILL.md`'s Ship/Handoff Harvest bullet overclaimed scanning "this project's own GitHub Issues," contradicting `self-improvement.md`'s own (accurate, narrower) Harvest section and the actual implementation | Principal (RED) | `SKILL.md` | **Fixed — represented by the implementing agent as landing in this PR; not yet visible in the working tree at consolidation time.** `SKILL.md`'s bullet corrected to match the narrower, actually-implemented scope already stated in `self-improvement.md`'s Harvest section — closes the 3-way drift by aligning the overclaiming document, not by building new scanning capability. |
| 8 | "What this is not → Not telemetry by default" heading asserted non-telemetry while the body described automatic, recurring, unprompted transmission once consented | Principal (AMBER) | `references/self-improvement.md` | **Fixed — represented by the implementing agent as landing in this PR; not yet visible in the working tree at consolidation time.** Heading softened (e.g. "No telemetry without consent, ever") so it no longer contradicts its own body. |

**Pending** (genuine follow-ups, not addressed in this PR — mechanical, no human judgment call
required):

| # | Finding | Raised by | Doc(s) | Action |
|---|---|---|---|---|
| 9 | "Hive Growth Loop" named before it's defined; the metaphor itself is never explained inline | Junior (AMBER) | `SKILL.md` | Add a one-clause inline definition at first use. |
| 10 | "Swarm" already had 3 meanings; this PR's "hive" and "node" add 2 more unreconciled overlapping metaphors — "node" duplicates `swarm.sh`'s own consistent term "stream" | Junior (AMBER) | `references/subagents.md`, `references/self-improvement.md` | Reuse "stream" instead of "node"; add a one-line swarm/hive/stream reconciliation note. |
| 11 | `references/issue-intake.md` never mentions the Hive Growth Loop or `.github/wgm-hive.yml`, and never cross-links back to `wgm-hermes`/`subagents.md` | Junior (AMBER), Senior (AMBER) | `references/issue-intake.md` | Add a top-of-file sentence naming its role as a Hive Growth Loop capture source, and add the missing cross-link. |
| 12 | Three different, non-matching wordings of "the one question wgm asks" (template comment, `SKILL.md` paraphrase, script's actual prompt string), plus an undocumented second ask-code-path in `harvest-hive.sh` | Junior (AMBER), Senior (GREEN — narrower template-vs-runtime instance of the same drift) | `assets/wgm-hive.template.yml`, `SKILL.md`, `scripts/harvest-hive.sh` | Designate one canonical wording and propagate it verbatim to all three; add a line explaining `harvest-hive.sh`'s own interactive ask exists as a standalone-invocation fallback (for when the script runs outside the normal Triage-already-asked flow), not a second competing trigger. |
| 13 | `consent:false`+`auto_report:true` is representable but a silent no-op no doc explains; `consent:true`+`auto_report:false` (correctly gated in code) is also never walked through in `self-improvement.md`'s "Consent & continuous mode" bullets | Junior (AMBER), Senior (AMBER) | `assets/wgm-hive.template.yml`, `references/self-improvement.md` | Add both missing combinations explicitly — the template's own comment already states the `true`+`false` combo's intended meaning; copy it into `self-improvement.md`. |
| 14 | Orphan term "levers" in `artifacts.md`'s "the hive consent/levers file" heading, never defined or reused | Junior (GREEN) | `references/artifacts.md` | Define it inline, or drop the word from the heading. |
| 15 | "Researched Hermes-style multi-agent pattern" (invoked 3×) and ".agent/.ai research" claims are never named/linked, breaking the repo's own external-research citation convention | Junior (AMBER), PM (AMBER) | `references/self-improvement.md`, `references/subagents.md`, `.github/agents/wgm-hermes.agent.md` | Name/link the actual source repo if one exists, or soften the wording (e.g. "inspired by the messenger-god framing") to not imply a specific, uncited precedent — do not invent a citation. |
| 16 | `self-improvement.md`'s own one-line flywheel summary wasn't updated for the new "Swarm harvest" section that now sits between Capture and Harvest | Junior (AMBER) | `references/self-improvement.md` | Add "swarm harvest" to the flywheel summary line. |
| 17 | `docs/plans/2026-07-06_HIVE_GROWTH_LOOP.md` is linked from only one file (`self-improvement.md`); `artifacts.md`/`subagents.md` describe the feature but never link back to "why"; separately, `docs/README.md`'s own "Plans & roadmap" list omits it entirely even though it indexes every older sibling | PM (AMBER); the `docs/README.md` index gap surfaced independently by wgm-docs-writer (README index check, above) | `references/artifacts.md`, `references/subagents.md`, `docs/README.md` | Add the missing cross-links from `artifacts.md`/`subagents.md`, and add the missing `docs/README.md` "Plans & roadmap" entry. |
| 18 | No worked example for the two most novel mechanics — anonymization and the consolidated swarm-memories file | Junior (AMBER) | `references/self-improvement.md` | Add one worked example for each, grounded in the script's actual behavior. |
| 19 | `docs/agent/lifecycle.md`'s phase table (Triage row omits the consent check; Ship row doesn't mention automatic reporting), `docs/operator/playbook.md`, and `docs/operator/running-the-loop.md`'s Swarm section are the "quick-skim" surfaces of this feature, and none of them mention the new consent question, automatic reporting, or memory-consolidation behavior | Junior (AMBER), Senior (AMBER ×2), PM (AMBER) | `docs/agent/lifecycle.md`, `docs/operator/playbook.md`, `docs/operator/running-the-loop.md` | Add short notes to all three. |
| 20 | `SKILL.md`'s own References section never names `scripts/harvest-hive.sh` (unlike every other primary script) and its assets-bullet enumeration never lists `assets/wgm-hive.template.yml` | Junior (GREEN), Senior (AMBER) — **severity dissent, see Dissent #3** | `SKILL.md` | Add both to the relevant References bullets. |
| 21 | `references/adr.md` never records the (now twice-used) `docs/plans/` precedent for wgm's own protocol-level design decisions, unlike the analogous docs/audit gitignore exception recorded in `artifacts.md` | Principal (AMBER), PM (AMBER) | `references/adr.md` | Add a short bullet mirroring the docs/audit carve-out precedent. |
| 22 | `artifacts.md`'s "the one exception" phrase is used twice for two different rules (the `.github/wgm-hive.yml` placement exception vs. the docs-audit git-tracking exception), which is brittle/easy to conflate, and doesn't acknowledge `.wgm/memories.md`'s placement already deviates from the greenfield-root split the same fixed way | Principal (GREEN/AMBER) | `references/artifacts.md` | Reword one of the two "the one exception" phrasings to disambiguate; add a one-line acknowledgment of the `memories.md` parallel. |
| 23 | Swarm harvest's token-budget math isn't reconciled — Capture claims the new sources "don't change how little of it wgm keeps around at once," but Swarm harvest unconditionally concatenates every stream's full memories file with no trim step, against the ~2000-token budget stated elsewhere | Principal (AMBER) | `references/self-improvement.md` | State plainly when/whether a trim rule fires relative to swarm consolidation, or soften the Capture claim to acknowledge consolidated files aren't yet trimmed to that budget. |
| 24 | PR #61's own "Notes for reviewers" text overstates "nothing changes for existing installs" — every existing project's next `/wgm` run will see a brand-new mandatory question and a new committed file regardless of answer | PM (AMBER) | PR #61 description | Correct the PR description text. |
| 25 | `.github/PULL_REQUEST_TEMPLATE.md`'s backpressure checklist has no line item for the mandatory docs-audit pass (ran it / deferred it), so a reviewer can't tell from the checklist alone whether this gate was satisfied | PM (AMBER) | `.github/PULL_REQUEST_TEMPLATE.md` | Add a checkbox line referencing `docs/audit/README.md`. |
| 26 | `sources:` field in `.github/wgm-hive.yml`/`assets/wgm-hive.template.yml` is documented as an actionable per-source toggle but is read by no shipped script (`harvest-hive.sh` only parses `consent`/`auto_report`; `swarm.sh` doesn't touch the file at all) — trimming it has zero observable effect, and it's unclear who owns enforcing it | Junior (RED), PM (AMBER) — **severity dissent, see Dissent #2** | `assets/wgm-hive.template.yml`, `references/artifacts.md` | Mark the field plainly as "accepted but not yet enforced by any script" in both the template's comment and `artifacts.md`, rather than implying it already gates anything. (Actually wiring per-source filtering into `harvest-hive.sh`/`swarm.sh` is a larger scope decision — not attempted here; see the note in Operator action #1's neighborhood.) |
| 27 | `harvest-hive.sh`'s fuzzy issue-dedup heuristic (normalize + substring + ≥3 shared-keyword match) has no stated limitation or caveat, unlike anonymization which repeatedly gets one | PM (AMBER) | `scripts/harvest-hive.sh` | Add a one-line caveat that this is a heuristic keyword-overlap match, not a guaranteed dedup — mirrors the existing "not a redaction guarantee" pattern. |
| 28 | The non-interactive-consent-fallback fix verified in "Fixed in this PR" #6 above also defuses the most dangerous form of PM's flagged race (N parallel `scripts/swarm.sh` worktree streams each independently reaching `harvest-hive.sh` with no consent file and no human present): the fixed path no longer writes anything, so streams can no longer race to persist a permanent `false`. What remains open post-fix is narrower and mechanical: no regression test exercises concurrent/parallel non-interactive invocations of `harvest-hive.sh`, and no doc sentence states that swarm streams degrade safely (decline-this-run-only, no file write) instead of racing. | PM (AMBER), reframed by wgm-docs-writer against the verified fix in #6 — this finding predates that fix and reasonably called for more caution at the time it was raised | `scripts/test-harvest-hive.sh`, `references/self-improvement.md` | Add a test case exercising 2+ concurrent non-interactive `harvest-hive.sh` invocations against a shared consent file; add a doc sentence describing the safe degrade. |
| 29 | The consent-trigger rule is restated near-verbatim in ≥5 places (`SKILL.md`, `self-improvement.md`, `artifacts.md`, `wgm-hive.template.yml`, `wgm-hermes.agent.md`) with already-detectable wording drift (see #12); the "four capture sources" catalog is independently duplicated in `self-improvement.md` prose, the template's list, and hardcoded again inside `harvest-hive.sh`'s own `consent_yaml()` heredoc, with no single source of truth | Senior (AMBER) | `SKILL.md`, `references/self-improvement.md`, `references/artifacts.md`, `assets/wgm-hive.template.yml`, `.github/agents/wgm-hermes.agent.md`, `scripts/harvest-hive.sh` | Designate `self-improvement.md` ("Consent & continuous mode") as canonical for the trigger wording and `wgm-hive.template.yml` as canonical for the sources list; have the other locations cite rather than restate them. |
| 30 | `references/subagents.md`'s Hive-courier bullet list omits the "anonymization is a first-pass deterministic scrub, not a redaction guarantee" caveat that `self-improvement.md` itself mandates be stated "wherever this step is described or implemented" | Senior (AMBER) | `references/subagents.md` | Add the caveat — it's already present verbatim in `wgm-hermes.agent.md`'s Limitations section, so this is a direct copy-over. |

### Operator actions
> These need a human decision, external confirmation, or access the agent doesn't have.

| # | Finding | Raised by | Doc(s) | Why it needs a human |
|---|---|---|---|---|
| 1 | "One lesson per report" is not implemented — `harvest-hive.sh` has no lesson-selection logic at all; it anonymizes and files the **entire** raw memories file as one issue, titled from only the first line (Senior verified this against a realistic 5-entry, 2-swarm-stream fixture: it became one issue titled after entry #1) | Senior (RED) | `scripts/harvest-hive.sh`, `references/self-improvement.md` | Deciding whether to actually build real per-lesson extraction is a product-scope decision, not a mechanical fix: it changes the script's behavior materially (what counts as one lesson's boundary; whether one run now files N issues instead of one; how that interacts with de-dup) — more like a feature commitment than a bug fix. An agent could mechanically document the current whole-file-dump behavior as an interim disclosure, but that doesn't resolve the underlying scope question, only the honesty of the current doc. |
| 2 | The mandatory first-question consent check isn't scoped away from read-only modes (`/wgm review`, `/wgm analyze only`) | Senior (AMBER) | `SKILL.md` | Deciding whether a read-only invocation should still trigger a file-writing, blocking consent question — and if not, exactly which modes are exempt — is a scope decision about what "read-only" is allowed to mean for this protocol, not a mechanical fix. |
| 3 | Two related, still-open reconciliation gaps between "Hermes does not ask the question itself" (`wgm-hermes.agent.md`, `references/subagents.md`) and the actual non-interactive/headless behavior of the mechanism that implements it: (i) even after the fix verified in Agent action #6, `harvest-hive.sh`'s *interactive* branch (stdin is a TTY, file absent) still asks the question directly from inside the script — a fallback path the docs never acknowledge exists outside the normal Triage-already-asked flow; (ii) at the protocol level, no doc states what `SKILL.md` Phase 0's own mandatory consent question should do when Triage itself runs fully unattended (Ralph-full / a headless coding-agent execution) — `references/stall-recovery.md`'s existing "unattended → preserve, revert, hand off" idiom is never referenced as the model to follow | Senior (RED — residual half of the finding fixed in Agent action #6), Principal (AMBER) | `.github/agents/wgm-hermes.agent.md`, `references/subagents.md`, `SKILL.md`, `scripts/harvest-hive.sh`, `references/stall-recovery.md` | Deciding the actual non-interactive protocol behavior — skip and default to declined? block until a human answers? defer entirely to a later interactive session? — is a genuine policy call with real adoption-rate consequences for the growth loop, not a mechanical doc fix. Related to, but distinct in scope from, Operator actions #2 and #5 above/below. |
| 4 | The Health-check guardrail's "3 self-referential PRs in a row" counting rule doesn't obviously count this PR's own kind of work (a large internal feature-engineering PR isn't named as either a "docs-audit pass" or a "Cross-pollinate PR," though arguably more self-referential than either) | Principal (AMBER) | `references/self-improvement.md` | Broadening the rule's scope — and deciding whether this PR itself should count against it — is a policy call about what the guardrail is actually meant to catch. |
| 5 | No carve-out exists for the degenerate case where the host project IS `agent-frontier/wgm` itself (the consent question being asked inside wgm's own repo, dogfooding on its own growth loop) — other artifacts in `references/artifacts.md` already special-case this repo (e.g. the docs/audit gitignore exception) | Principal (GREEN) | `references/artifacts.md` | Deciding what wgm's own repo's consent answer should be, and whether it needs its own documented carve-out, is the same kind of self-referential policy call this repo already makes deliberately elsewhere — not a mechanical fix. |
| 6 | Anonymization scrub **verified to over-redact** benign content — run directly against ordinary prose citing public repos, a fraction and two real public repo names (`golang/go`, `3/4`, `facebook/react`) were all wrongly redacted; the docs only ever disclose under-redaction risk, never over-redaction | PM (AMBER, verified) | `scripts/harvest-hive.sh` | Tightening the repo/org-shaped pattern to admit these cases risks quietly reopening the case-sensitivity/precision gap just fixed in Agent action #4 — the right precision/recall balance for a privacy-relevant, publicly-publishing redaction feature is a quality-bar judgment call a human should own, not a regex tweak an agent should make unilaterally. |

### Dissent
> Disagreements between personas, preserved rather than averaged away.

| Finding | Persona A view | Persona B view | Preserved as |
|---|---|---|---|
| Stale "eleven→twelve" subagent count + tree-comment omissions in `README.md`/`docs/README.md` | Junior: **RED** — this is the newcomer's actual entry point, it flatly contradicts `SKILL.md`, and it's a regression on an already-fixed discipline (`docs/audit/2026-07-05T0622Z_pr41-45-post-merge-audit.md` #8) — treated as a blocker. | Senior: **AMBER** — filed under "Completeness/Maintainability," not "Correctness." PM: **AMBER** — same regression class, but rated a completeness gap rather than a blocker. | Agent action #1 — fixed in this PR regardless of the severity split. Kept here because Junior weighed the newcomer-facing contradiction and the regression-on-a-fixed-discipline angle heavily enough to call it a blocker, while Senior and PM (independently) rated the same underlying fact AMBER; the discipline here is to preserve that three-way split rather than silently pick one severity. |
| `sources:` field documented as an actionable per-source control but read by no shipped script | Junior: **RED** — verified zero observable effect from trimming the list; treated as actively misleading, not just incomplete. | PM: **AMBER** — same fact, framed as an ownership/accountability gap ("unclear who's responsible for enforcing it") rather than a correctness blocker. | Agent action #26 (pending) — both views converge on the same remedy (disclose the field's current inert status plainly); kept here because Junior's "zero observable effect" framing and PM's "unclear ownership" framing assign different severities to the same fact, and neither should be silently dropped in favor of the other. |
| `SKILL.md`'s own end-of-file artifact/References inventory omits `scripts/harvest-hive.sh` (and, per Junior, `assets/wgm-hive.template.yml`) | Junior: **GREEN** — filed as a minor completeness nit alongside the template omission. | Senior: **AMBER** — filed under "Completeness/Maintainability," treating the missing script citation as a real maintainability gap ("unlike every other primary script"). | Agent action #20 (pending) — both personas want the same one-line fix; kept here because Junior treated it as cosmetic while Senior treated the same omission as a real drift risk worth an AMBER, not because the fix itself is contested. |

## Verdict history
Previous entry: [2026-07-06T0122Z — Growth health-check + self-improvement guardrail branch —
GREEN](2026-07-06T0122Z_growth-health-check-and-guardrail.md).

This run (PR #61, an unrelated feature branch — not a like-for-like comparison to the entry above):
**AMBER** — regressed relative to the immediately preceding GREEN entry, on substance rather than
just sequence. The preceding entry's GREEN rested specifically on "every finding this round... was
fixed in the same branch before the PR opened... nothing outstanding blocks Ship." This run does not
meet that bar: six of eight RED findings were fixed pre-merge and verified in this pass (a real,
material strength — see "Fixed in this PR" above), but one RED-severity implementation gap (real
per-lesson report splitting) is knowingly deferred as an Operator decision rather than fixed, one RED
finding is only half-closed (the consent-persistence bug is fixed; the "Hermes does not ask"
documentation contradiction is not fully resolved — see Operator action #3), and roughly two dozen
mechanical Agent actions surfaced by this pass remain genuinely open, unlike the fully-closed prior
round. None of this blocks Ship on its own (Operator actions are a legitimate, expected end-state
under this repo's own classification discipline, not a failure to act), but the paper trail should
not read as "fully closed" when it isn't.
