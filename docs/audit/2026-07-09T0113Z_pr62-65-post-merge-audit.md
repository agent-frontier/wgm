# Docs Audit Report — 2026-07-09T0113Z — pr62-65-post-merge-audit

> Generated by the wgm docs-audit swarm (`wgm-docs-junior`, `wgm-docs-senior`, `wgm-docs-principal`,
> `wgm-docs-pm` → consolidated by `wgm-docs-writer`). This is the qualitative pass; structural checks
> (required files, links, fences, placeholders) live separately in `scripts/check-docs.sh`.

- **Scope:** PRs #62, #63, #64, #65 — the diff `7d72693..HEAD`, **24 files changed** (890
  insertions, 76 deletions) — everything merged to `main` since the last consolidated audit
  ([`docs/audit/2026-07-06T0602Z_hive-growth-loop-pr61.md`](2026-07-06T0602Z_hive-growth-loop-pr61.md),
  which covered PR #61). Per-PR breakdown (verified via `git log --merges` / `git diff --stat` /
  `gh pr view`):
  - **#62** `fix/hive-growth-loop-audit-followups` (merged `2026-07-06T07:47:13Z`, 23 files) — lands
    the highest-confidence findings from the PR #61 audit itself: `harvest-hive.sh`'s
    non-interactive consent-persistence fix, case-insensitive/Windows-path/hostname anonymization,
    `swarm.sh`'s post-swarm Hermes dispatch, `SKILL.md`'s Harvest-bullet correction, CI enforcement
    for `test-harvest-hive.sh` — and files the PR #61 report/index themselves
    (`docs/audit/2026-07-06T0602Z_hive-growth-loop-pr61.md`, `docs/audit/README.md`).
  - **#63** `docs/harvest-learn-issue-60-deploy-vs-ci` (merged `2026-07-06T07:52:39Z`, 4 files) —
    promotes `[learn]` issue #60 (closed) into `references/ralph-loop.md`'s "Backpressure in depth"
    (a deploy-pipeline-distinct-from-per-PR-CI check), `SKILL.md`, `references/heuristics.md`,
    `references/self-improvement.md`.
  - **#64** `docs/local-models-token-budget` (merged `2026-07-08T23:24:45Z`, 5 files) — new
    `references/local-models.md` (small-context-model token-budget playbook), cross-linked from
    `SKILL.md`, `references/ralph-loop.md`, `references/memory-patterns.md`,
    `docs/operator/running-the-loop.md`.
  - **#65** `feature/skillopt-grade-evals` (merged `2026-07-09T00:21:03Z`, 10 files) —
    `scripts/grade-evals.sh` + `scripts/test-grade-evals.sh` (SkillOpt-inspired live grading +
    non-regression gate), `docs/plans/2026-07-08_SKILLOPT_ADOPTION.md` (the ADR), plus `SKILL.md`,
    `references/evals.md`, `references/heuristics.md`, `CONTRIBUTING.md`, `Makefile`,
    `.github/workflows/ci.yml`.

  A **~2.5-day gap** separates #63 (2026-07-06) from #64 (2026-07-08) with no consolidated
  docs-audit pass and no recorded batching justification in either PR body — see PM's finding and
  Operator action #3 below.
- **Track:** Standard.
- **Trigger:** Ship/Handoff-style consolidated pass, batched across 4 PRs — at the top of
  `references/docs-audit.md`'s own ~3-5 PR batching cap.
- **Overall verdict:** **RED** — regressed from the previous entry's AMBER. Two independent
  RED-severity signals survive this pass, neither remediated (this is a report-only consolidation;
  no fix has been applied in this run, unlike several earlier post-merge audits that fixed everything
  mechanical before filing): (1) Senior verified a direct, live factual self-contradiction between
  `references/self-improvement.md:251` and `assets/wgm-hive.template.yml:25-26` over whether the
  `sources:` field does anything — textbook RED under this project's own taxonomy ("actively wrong or
  misleading," not just incomplete); (2) Principal's dissenting RED on the `references/local-models.md`
  tree-comment gap is not a routine completeness nit but a **documented third occurrence** of a
  defect class this project's own paper trail already rated RED once (`docs/audit/2026-07-05T0622Z_pr41-45-post-merge-audit.md`
  finding #8) and partially-fixed-then-regressed once more (PR #61) — a pattern that has now twice
  survived a dedicated fix-and-declare-done cycle. `references/docs-audit.md` itself defines what the
  index's "verdict" column means: **"one-line verdict (worst severity found)"** — not an average, not
  a majority vote across the ~20 AMBER/GREEN findings this round. Per that explicit rule, and per the
  dissent-preservation discipline that a minority-flagged severity is never silently smoothed into the
  majority label, the worst severity found this round is RED (independently, twice over), so the
  overall verdict is RED. This is compounded by a real process-governance gap PM surfaced: PR #64 and
  PR #65 shipped with zero acknowledgment of the mandatory docs-audit checklist line, silently
  breaching `references/docs-audit.md`'s own "batching must be explicit, not silently practiced" rule
  for ~2.5 days — a gap only closed by this pass running now. None of this changes the fact that the
  concrete remedies below are small and mechanical (see Agent actions) — but the paper trail must
  report the state of the tree as it stands at filing time, not the state it will be in once those
  fixes land.

## Persona findings

### Junior developer — clarity & onboarding
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `README.md:197`, `SKILL.md:295`, `SKILL.md:356`, `docs/README.md:61`, `docs/README.md:80`, `assets/wgm-hive.template.yml:30` | The prior audit (PR #61) flagged "node" as an unreconciled synonym for `scripts/swarm.sh`'s own term "stream" (AMBER). This PR range fixes it correctly in `references/self-improvement.md`/`references/subagents.md`/`.github/agents/wgm-hermes.agent.md` — even adding the exact terminology box the old audit asked for ("swarm" = mechanism, "stream" = one worktree, "hive" = shared upstream) — but never swept the rest of the doc set. The six locations above (verified by grep) still say "node"/"swarm-node," including in `README.md` and `SKILL.md`, the two most likely newcomer entry points, both of which were *themselves edited elsewhere in this same diff* but not on these lines. | AMBER | Sweep the remaining "node"/"swarm-node" occurrences in these four files to "stream," matching the fix already applied to `self-improvement.md`. |
| `README.md` | The "Repository layout" ASCII-tree's `references/` line enumerates all reference docs by name (21 names listed) but wasn't updated for the new `references/local-models.md` (22 files exist). | AMBER | Add `local-models` to the `references/` tree-comment line. |
| `docs/README.md` | The newest-first "Plans & roadmap" list runs through 2026-07-06 but omits the new `docs/plans/2026-07-08_SKILLOPT_ADOPTION.md` — the newest plan doc, cross-linked from three other places in this same PR range. | AMBER | Add a 2026-07-08 bullet for the SkillOpt ADR to the Plans & roadmap list. |
| `CONTRIBUTING.md` | The backpressure-suite line reads `bash scripts/test-harvest-hive.sh # harvest-hive.sh anonymize + consent harness (9 cases)`, but this same diff added a new numbered case to that script. Running it now prints case numbers `# 1)` through `# 10)`, not 9. | AMBER | Update the case count in `CONTRIBUTING.md`'s comment (verify the true count). |
| `references/local-models.md` | The one concretely-given, copy-pasteable command for measuring token cost (`python3 -c "import tiktoken; ..."`) silently depends on the third-party `tiktoken` PyPI package; nothing says to install it first, and the repo has no `requirements.txt`/`pyproject.toml`. | AMBER | Add a one-line `pip install tiktoken` prerequisite immediately before the command. |
| `docs/plans/2026-07-08_SKILLOPT_ADOPTION.md` | Titled "# ADR: adopt SkillOpt's grading discipline..." — "ADR" is never expanded inline anywhere in the file, only via a link to `references/adr.md`. | GREEN | Expand "Architecture Decision Record" on first use, or switch to a descriptive title like its 12 siblings. |
| `references/self-improvement.md`, `assets/wgm-hive.template.yml` | The new "Consent & continuous mode" intro claims to be "the canonical source for the one-time consent-question wording" but never states a literal quoted prompt itself; `wgm-hive.template.yml` still carries its own separately-worded quoted prompt with no cross-reference back. | GREEN | Either move the literal quoted prompt into `self-improvement.md` and have the template cite it, or soften the claim to describe behavior only. |

### Senior developer — correctness, completeness, maintainability
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `references/self-improvement.md` (L251), `assets/wgm-hive.template.yml` (L25-26) | Direct factual contradiction between two docs edited in this same PR range: self-improvement.md's "Consent & continuous mode" says "the `sources:` list can trim which channels feed" local Capture/Harvest, while wgm-hive.template.yml says the list "is accepted here but NOT YET enforced by any shipped script, so trimming it currently has no observable effect." Verified against `scripts/harvest-hive.sh`: `sources:` is never parsed back from an existing file, only written into freshly-generated ones. self-improvement.md's claim is the false one. | RED | Correct self-improvement.md's line to match wgm-hive.template.yml's accurate wording, or implement real per-source filtering. |
| `SKILL.md` (L356), `README.md` (L197), `docs/README.md` (L61, L80) | self-improvement.md adds an explicit terminology ruling replacing "node" with "stream," propagated to `subagents.md`/`wgm-hermes.agent.md`. SKILL.md's own References bullet, README.md's flywheel bullet, and docs/README.md's mentions still say "swarm node(s)." | AMBER | Sweep the four remaining "swarm node(s)" instances to "swarm stream(s)." |
| `references/local-models.md` | States precise tiktoken-measured figures ("SKILL.md is 6,820 tokens," "references/*.md combined is 49,766 tokens"). Re-running the exact command the doc prescribes now gives 7,026 and 51,530 respectively — already 3-6% higher, because PR #65 added ~30 lines to SKILL.md without this file's numbers being refreshed. | AMBER | Drop the hardcoded figures in favor of "measure it yourself" only, or caption them with the commit/date captured. |
| `CONTRIBUTING.md` (L46) | The backpressure-suite line for `scripts/test-harvest-hive.sh` still says "(9 cases)," but the script now produces 10 "ok:" lines. | AMBER | Bump "(9 cases)" to "(10 cases)." |
| `references/heuristics.md` (L15-16) | Header self-reports "38 entries and counting." Actual count (`grep -c '^\- \*\*Heuristic:\*\*'`) is 41. | AMBER | Update to 41, or replace the hardcoded number with a pointer to a live count (`grep -c`). |
| `docs/README.md` (Map table), `README.md` (repository-layout tree) | `references/local-models.md` is the only one of 22 `references/*.md` files absent from both docs/README.md's "Map" table and README.md's `references/` bullet list. | AMBER | Add a Map row (docs/README.md) and a references/ bullet mention (README.md) for `local-models.md`. |
| `Makefile` (L37-42) | The `test` target's doc-comment and body were edited to add `grade-evals`, but it still never calls `scripts/test-harvest-hive.sh`, even though `.github/workflows/ci.yml` and CONTRIBUTING.md's "these are exactly what CI runs" both include it. `make test`/`make validate` silently diverges from CI's real coverage. | AMBER | Add `bash scripts/test-harvest-hive.sh` to the `test` target. |
| `.github/PULL_REQUEST_TEMPLATE.md` | "Backpressure checks" still omits `check-evals.sh`, `test-loop.sh`, `test-swarm.sh`, `test-devcontainer.sh`, `test-harvest-hive.sh`, and the brand-new `test-grade-evals.sh` — all of which CONTRIBUTING.md calls "exactly what CI runs. All must be green." | AMBER | Sync the checklist to CONTRIBUTING.md's current list (or replace with one "ran the CONTRIBUTING.md backpressure suite" checkbox), and move the docs-audit line to its own section. |
| `docs/operator/playbook.md` | The per-build checklist's new step 2 ("expect the consent question first…") has no matching row in the "Per-gate PASS/FAIL cheat-sheet" — every other named phase has one. | AMBER | Add a Triage/consent row, or state explicitly this step has no FAIL branch and is intentionally excluded. |
| `references/heuristics.md` (L224) | The new SkillOpt entry's Provenance field is the only one of 41 that embeds a markdown hyperlink instead of the file's established plain-backtick-citation style. | GREEN | Match the surrounding plain-citation style. |
| `scripts/grade-evals.sh`, `scripts/test-grade-evals.sh`, `references/evals.md`, `CONTRIBUTING.md` | Ran live (7/7 passed matching CONTRIBUTING.md's "(7 cases)"); design, flags, exit codes, "never wired into CI" all check out exactly as documented. | GREEN | None. |

### Principal developer — architecture, strategic fit, consistency
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `README.md:268` | The `references/` repo-layout tree-comment was a complete 1:1 enumeration before this round; PR #64 added `references/local-models.md` but never added it here. This is the *identical* defect class the PR #61 audit rated RED (first fixed at PR41-45 audit finding #8, regressed and re-fixed at PR #61) — this is its third occurrence. | RED | Add `local-models` to line 268 now. Given 2+ prior regressions of the same pattern, also add a mechanical check to `scripts/check-docs.sh` that fails if any `references/*.md` filename is absent from this README line. |
| `SKILL.md:295,356`, `README.md:197`, `assets/wgm-hive.template.yml:30`, `docs/README.md:61,86` | The PR #61 audit's AMBER "node" finding was only partially fixed — 6 instances of stale "node" phrasing survive across SKILL.md (twice), README.md, wgm-hive.template.yml, and docs/README.md (twice) — more unfixed instances than fixed ones. | AMBER | Grep-replace "node" → "stream" in the 6 remaining call sites; have other files cite self-improvement.md as canonical rather than restate. |
| `docs/plans/2026-07-08_SKILLOPT_ADOPTION.md` (Context), `CONTRIBUTING.md:8-16` | The ADR elevates three explicit constraints ("stay portable/lean," "prefer the simplest elegant solution," "rule of 3") as the stated reason 2 of 3 alternatives were rejected, but sources them only as "session preferences"; none appears anywhere durable in CONTRIBUTING.md's Ground rules or any references/*.md file. | AMBER | Add one bullet to CONTRIBUTING.md's Ground rules capturing "prefer zero new runtime dependencies; keep changes to the smallest number of files/commands," sourced from this ADR. |
| `references/heuristics.md:219-228` | The "Comparative & hard-to-test scoring" heading groups the new SkillOpt/grade-evals.sh heuristic (boolean-per-assertion pass_rate 0.0-1.0) directly alongside scoring.md's continuous 0-100 LLM-judge satisfaction score under the shared word "score" — a different scale/mechanism the shared vocabulary implies are the same. | AMBER | Reword the heuristic bullet to say "pass_rate" instead of "score," or add a clause distinguishing it from scoring.md's 0-100 score. |
| `references/local-models.md` (Cross-links), `references/subagents.md:138-141` | local-models.md's context-size-tier reframing of the frugal/main model-selection flags has no cross-link to/from subagents.md's "Model selection" section, where the same flag pair is discussed along the cost axis. No contradiction, just an asymmetric missing cross-link. | GREEN | Add references/subagents.md (Model selection) to local-models.md's Cross-links, and a pointer the other way. |
| `docs/plans/2026-06-16_RALPH_LANDSCAPE.md`, `references/heuristics.md:219-228`, `docs/plans/2026-07-08_SKILLOPT_ADOPTION.md` | Mandatory sweep: heuristics.md's Provenance ledger properly records PR #65's SkillOpt adoption; no other file this round introduces an un-logged new external-tool citation. | GREEN | None required; optionally a one-line "round 5" addendum naming SkillOpt, purely as a nice-to-have. |

### Project manager — status, risk, traceability
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `docs/plans/2026-07-08_SKILLOPT_ADOPTION.md` | States its own status unambiguously (`**Status:** accepted`), explicit about what's declined vs. built. Matches SKILL.md/evals.md's claims about the script's maturity word-for-word. | GREEN | None — good template for future ADRs. |
| `references/evals.md`, `SKILL.md`, `CONTRIBUTING.md`, `README.md`, `scripts/grade-evals.sh` | Cross-checked the "maturity" claim against reality: ran `scripts/test-grade-evals.sh` live (7/7 pass); confirmed `test-grade-evals.sh` (not `grade-evals.sh` itself) is wired into Makefile/CI exactly as every doc claims. No overclaim that the API-cost script is CI-safe. | GREEN | None — status claim is verified. |
| `references/heuristics.md` (PR #63 addition) | Traceable: `gh issue view 60` confirms issue #60 is CLOSED, body matches the heuristic's "Why" field. Both "Landed in" cross-references present and verified. | GREEN | None — model harvest. |
| `docs/audit/README.md` | Confirmed current: last entry still PR #61/AMBER. Grepped entire repo for #62/#63/#64/#65 — zero docs anywhere falsely claim these were audited. | GREEN | None on the index itself. |
| `.github/PULL_REQUEST_TEMPLATE.md` + PR #63/#64/#65 bodies | PR #62 added a mandatory checklist line ("Docs-audit swarm ran, or explicitly deferred/batched per references/docs-audit.md"). PR #63 used it correctly (explicitly declared deferred). PR #64 and PR #65 contain zero mentions of "docs-audit"/"audit" anywhere in their bodies — neither used the checklist at all. A ~2.5-day gap between PR#62/#63 (07-06) and PR#64/#65 (07-08) suggests separate sessions elapsed with no consolidated pass and no recorded justification, contradicting docs-audit.md's own rule that batching must be explicit, not silently practiced. | AMBER | (1) This session's report should explicitly close the #62-#65 gap and note the cadence lapse. (2) Consider a cheaper enforcement point than free-text PR bodies — e.g., an "audit debt: N PRs / since YYYY-MM-DD" line in docs/audit/README.md itself. |
| `assets/wgm-hive.template.yml` | `sources:` list honestly documented as accepted-but-not-enforced. Verified against harvest-hive.sh: never parsed back, only written. Self-disclosed limitation is accurate. | GREEN | None. |
| `docs/plans/2026-06-16_RALPH_LANDSCAPE.md` | Mandatory sweep: SkillOpt is logged in heuristics.md's Provenance field; no un-logged new citation found elsewhere. | GREEN | None required. |
| Open-issue state, subagent count | `gh issue list --state open` returns zero, no doc contradicts. `ls .github/agents/` = 12, matching "twelve role-specialized subagents" claimed consistently. | GREEN | None — both counts verified accurate. |

## Consolidated report (wgm-docs-writer)

**No fixes were applied in this pass.** This is a report-only consolidation of four already-completed
persona reviews; every Agent action below is an open, queued follow-up, not something already done —
unlike some earlier post-merge audits in this index that fixed everything mechanical before filing.

### README index check
- **`README.md`'s "Repository layout" tree-comment (`references/` line, L268)** was a complete 1:1
  enumeration of all reference docs before this PR range (21 names for 21 files) and has silently
  fallen out of sync with the filesystem: `references/local-models.md` (added in PR #64) is the 22nd
  file and is not listed. Verified: `ls references/*.md | wc -l` → 22; the tree-comment line names
  exactly 21. This line's whole job is exhaustive enumeration, so this is a genuine, novel regression
  — see Agent actions #2/#3 and the Dissent entry below for the severity split on how serious this
  specific regression is given its history.
- **`docs/README.md`'s "Map" table**, by contrast, is a long-standing **curated subset** of
  `references/*.md` — only 7 of 22 files (`devcontainers`, `docs-audit`, `evals`, `issue-intake`,
  `self-improvement`, `subagents`, `trigger-eval`, the "deeper mechanics" docs the file's own intro
  paragraph names), not a 1:1 index. It already excluded 14 other reference files
  (`adr`, `artifacts`, `gene-transfusion`, `grilling`, `hard-to-test-domains`, `heuristics`,
  `memory-patterns`, `PLUGIN_PROTOCOL`, `plugin-integration`, `ralph-loop`, `scenarios`, `scoring`,
  `stall-recovery`, `validation-env`) before this PR range even landed — a design PR #64's own
  description explicitly reaffirms ("`docs/README.md`'s Map table is intentionally untouched — it's
  a curated subset"), and one this same paper trail already ruled on for the identical pattern
  (`docs/audit/2026-07-05T1003Z_pr50-53-post-merge-audit.md`: "the Map already excludes ~13 other
  reference files well before this session — not a regression"). Senior's finding that
  `local-models.md` is "the only one of 22... absent from... the Map table" is technically true of
  the observable fact but frames it as a fresh, comparable gap; checked against the Map's actual
  contents, `local-models.md`'s absence is indistinguishable from 14 siblings excluded by
  long-standing design. **Consolidation call: the Map-table half of this finding is not carried into
  the Agent actions table as new work — only the README.md tree-comment line (which genuinely was
  exhaustive) is.** This mirrors the PR50-53 report's ruling on the same pattern in the same table.
- **`docs/README.md`'s "Plans & roadmap" list** indexes 6 of the 13 files under `docs/plans/` — also
  a curated, newest-oriented list, not a full 1:1 index (it already omits `2026-06-12_LAUNCH_AUDIT_PLAN.md`,
  `2026-06-12_PLAN.md`, `2026-06-17_PLAN.md`, and three `2026-07-04_*` files, none of which is new to
  this PR range or raised by any persona — out of scope here). What **is** new and in-scope: the list
  stops at 2026-07-06 and never gained a bullet for `docs/plans/2026-07-08_SKILLOPT_ADOPTION.md` — the
  newest plan doc, cross-linked from three other places landing in this same PR range (`SKILL.md`,
  `references/heuristics.md`, `references/evals.md`). Unlike the Map table, this list's own organizing
  principle is newest-first chronological completeness (established precedent:
  `docs/audit/2026-07-06T0602Z_hive-growth-loop-pr61.md` finding #17, "indexes every older sibling"),
  so this omission is a genuine gap, not curation. See Agent action #5.
- **`docs/audit/README.md`** (the audit index itself) is confirmed current as of this pass: its last
  entry is still `2026-07-06T0602Z` / PR #61 / AMBER, and a repo-wide grep for "#62"/"#63"/"#64"/"#65"
  turns up no doc anywhere falsely claiming any of these four PRs was already audited (PM's finding,
  independently spot-checked by the writer). It does **not yet** have a row for **this** report —
  that row is the next required step once this file is saved (see Verdict history).
- **No other doc found to exist-but-be-unindexed** beyond the two genuine gaps above (the tree-comment
  line, the Plans & roadmap list). `docs/operator/*.md` (7/7 files) and `docs/agent/*.md` (5/5 files)
  are both fully indexed in `docs/README.md`'s Map table, and `.github/agents/*.agent.md`'s count (12)
  matches every prose claim of "twelve role-specialized subagents" checked this round (PM's finding,
  re-verified: `ls .github/agents/ | wc -l` → 12).

### Agent actions
> The agent can execute these directly and deterministically — no human judgment required. None of
> these has been executed yet; this pass is report-only.

| # | Finding | Raised by | Doc(s) | Action |
|---|---|---|---|---|
| 1 | Stale "node"/"swarm-node" terminology survives in 6 locations even though `self-improvement.md`'s "Consent & continuous mode" already fixed the canonical wording to "stream" this same PR range (and added the swarm/stream/hive terminology box the PR #61 audit asked for). Writer-verified canonical location list: `README.md:197`, `SKILL.md:295`, `SKILL.md:356`, `docs/README.md:61`, `docs/README.md:80`, `assets/wgm-hive.template.yml:30` (Senior's list of 4 is a subset of the same 6; Principal's citation of `docs/README.md:86` is a one-line-off citation slip for the same `:80` occurrence, reconciled here). | Junior, Senior, Principal | `README.md`, `SKILL.md`, `docs/README.md`, `assets/wgm-hive.template.yml` | Replace "node(s)"/"swarm-node" with "stream(s)" at all 6 locations, matching `references/self-improvement.md`'s already-fixed wording; have each site cite `self-improvement.md` as canonical rather than restate the terminology. |
| 2 | `references/local-models.md` (the 22nd `references/*.md` file, added in PR #64) is missing from `README.md`'s repository-layout tree-comment line (L268) — the one enumeration that was a genuine 1:1 list before this round. **Severity dissent — see Dissent table below**, preserved rather than averaged. | Junior (AMBER), Senior (AMBER — also flagged the docs/README.md Map table, see README index check above for why that half isn't carried forward as new work), Principal (RED — 3rd occurrence of a defect class rated RED once before) | `README.md` | Add `local-models` to line 268's enumeration. |
| 3 | Principal's proposed structural fix for #2: this is the *third* occurrence of the identical "tree-comment enumeration drifts from new reference files" defect (1st: `docs/audit/2026-07-05T0622Z_pr41-45-post-merge-audit.md` finding #8, RED; 2nd: PR #61, partially fixed then regressed). Two manual fix-and-declare-done cycles have not held. | Principal | `scripts/check-docs.sh` | Add a mechanical check: fail if any `references/*.md` filename is absent from `README.md`'s repository-layout tree-comment line, so a 4th recurrence is caught automatically instead of by a 4th manual audit pass. |
| 4 | Direct, live factual self-contradiction between two docs edited in this same PR range: `references/self-improvement.md:251` says "the `sources:` list can trim which channels feed" local Capture/Harvest; `assets/wgm-hive.template.yml:25-26` says the same list "is accepted here but NOT YET enforced by any shipped script, so trimming it currently has no observable effect." Writer independently re-verified against `scripts/harvest-hive.sh`: `sources:` appears exactly once in the script, inside the heredoc that writes a **fresh** consent file — it is never parsed back from an existing one. `self-improvement.md`'s claim is the false one; `wgm-hive.template.yml`'s is accurate (independently corroborated by PM's own verification of the same script). | Senior (RED), corroborated by PM (verified the template's side is accurate) | `references/self-improvement.md` | Correct `self-improvement.md:251` to match `wgm-hive.template.yml`'s accurate "accepted but not yet enforced" wording. (Whether to actually build real per-source filtering is a separate, larger decision — see Operator action #1.) |
| 5 | `docs/README.md`'s newest-first "Plans & roadmap" list runs through 2026-07-06 but omits `docs/plans/2026-07-08_SKILLOPT_ADOPTION.md` — the newest plan doc, cross-linked from `SKILL.md`, `references/heuristics.md`, and `references/evals.md` in this same PR range. | Junior | `docs/README.md` | Add a 2026-07-08 bullet for the SkillOpt ADR, matching the existing entry format. |
| 6 | `CONTRIBUTING.md:46` says `bash scripts/test-harvest-hive.sh # ... (9 cases)`. Junior and Senior each re-ran the harness and independently reported "now 10." Writer re-ran it live during consolidation: `bash scripts/test-harvest-hive.sh 2>\&1 \| grep -c '^ok'` → **11**. Reconciled: the script's own comments number cases `# 1)` through `# 10)`, but case 2 has an unnumbered "2b" sub-case (`case-insensitive / Windows-path anonymization`) that emits its own separate `pass`/`fail` assertion — an easy miscount if counting only the numbered comments, which is almost certainly how both personas independently landed on 10. | Junior, Senior (writer-corrected count) | `CONTRIBUTING.md`, `scripts/test-harvest-hive.sh` | Update `CONTRIBUTING.md:46`'s count to "(11 cases)". Also give the "2b" sub-case its own top-level number (renumbering the rest) so a future headcount from the comments alone matches the real output. |
| 7 | `references/local-models.md`'s one concretely-given, copy-pasteable token-measurement command (`python3 -c "import tiktoken; ..."`) depends on the third-party `tiktoken` PyPI package with no install instruction anywhere in the file; the repo has no `requirements.txt`/`pyproject.toml` (verified absent at repo root). | Junior | `references/local-models.md` | Add a one-line `pip install tiktoken` prerequisite immediately before the command. |
| 8 | `references/local-models.md` states hardcoded tiktoken-measured figures ("SKILL.md is 6,820 tokens," "references/*.md combined is 49,766 tokens"). Writer re-ran the doc's own prescribed command against the current tree: `SKILL.md` → **7,026** tokens; `references/*.md` combined → **51,530** tokens — confirming Senior's "already 3-6% higher" almost exactly (+3.0% and +3.5% respectively), because PR #65 added ~30 lines to `SKILL.md` after this file's numbers were captured. | Senior (writer-reverified) | `references/local-models.md` | Drop the hardcoded figures in favor of "measure it yourself" only, or caption them with the commit/date they were captured at. |
| 9 | `references/heuristics.md:15-16` self-reports "38 entries and counting." Writer-verified actual count (`grep -c '^\- \*\*Heuristic:\*\*' references/heuristics.md`): **41**. | Senior | `references/heuristics.md` | Update the header to 41, or replace the hardcoded number with a pointer to a live count. |
| 10 | `Makefile`'s `test` target (L41-46) doc-comment and body were edited this PR range to add `grade-evals`, but never call `scripts/test-harvest-hive.sh` — writer-confirmed via direct read of the `test:` recipe — even though `.github/workflows/ci.yml` and `CONTRIBUTING.md`'s "these are exactly what CI runs" both include it. `make test`/`make validate` silently diverges from CI's real coverage. | Senior | `Makefile` | Add `bash scripts/test-harvest-hive.sh` to the `test` target's body and doc-comment. |
| 11 | `.github/PULL_REQUEST_TEMPLATE.md`'s "Backpressure checks" list (writer-confirmed via direct read) omits `check-evals.sh`, `test-loop.sh`, `test-swarm.sh`, `test-devcontainer.sh`, `test-harvest-hive.sh`, and the brand-new `test-grade-evals.sh` — all six of which `CONTRIBUTING.md` calls "exactly what CI runs. All must be green." | Senior | `.github/PULL_REQUEST_TEMPLATE.md` | Sync the checklist to `CONTRIBUTING.md`'s current backpressure-suite list (or collapse to one "ran the CONTRIBUTING.md backpressure suite" checkbox), and move the existing docs-audit checklist line to its own section for visibility. |
| 12 | `docs/operator/playbook.md`'s per-build checklist step 2 ("expect the consent question first…") has no matching row in the "Per-gate PASS/FAIL cheat-sheet" table (writer-confirmed: the cheat-sheet lists only Grill-exit, Plan-exit, Preflight, Iteration-exit, Ship/Handoff) — every other named phase has one. | Senior | `docs/operator/playbook.md` | Add a Triage/consent row to the cheat-sheet, or state explicitly that this step has no FAIL branch and is intentionally excluded. |
| 13 | `references/heuristics.md:224`'s new SkillOpt entry's Provenance field is the only one of 41 that embeds a markdown hyperlink (`` [`microsoft/SkillOpt`](https://github.com/microsoft/SkillOpt) ``) instead of the file's established plain-backtick-citation style (writer-confirmed by sampling other Provenance fields, e.g. `` `Aider-AI/aider` ``, `` `foundatron/octopusgarden` ``, none of which hyperlink). | Senior | `references/heuristics.md` | Reformat to plain-backtick style to match every other Provenance field. |
| 14 | `docs/plans/2026-07-08_SKILLOPT_ADOPTION.md`'s title ("# ADR: adopt SkillOpt's grading discipline, not the SkillOpt package") never expands "ADR" inline — only reachable via a link to `references/adr.md`. | Junior | `docs/plans/2026-07-08_SKILLOPT_ADOPTION.md` | Expand "Architecture Decision Record" on first use, or switch to a descriptive title matching its 12 `docs/plans/` siblings. |
| 15 | `references/self-improvement.md`'s "Consent & continuous mode" intro claims to be "the canonical source for the one-time consent-question wording" but never states the literal quoted prompt itself; `assets/wgm-hive.template.yml` carries its own, separately-worded quoted prompt with no cross-reference back to the claimed canonical source. | Junior | `references/self-improvement.md`, `assets/wgm-hive.template.yml` | Move the literal quoted prompt into `self-improvement.md` and have the template cite it verbatim, or soften the "canonical source" claim to describe behavior only. |
| 16 | `references/heuristics.md`'s "Comparative & hard-to-test scoring" heading groups the new SkillOpt/`grade-evals.sh` heuristic under the shared word "score," but writer-verified `scripts/grade-evals.sh:144` computes `pass_rate` as a plain `%.4f`-formatted fraction (0.0000-1.0000) — a different scale and mechanism from `references/scoring.md`'s continuous **0-100** LLM-judge score (confirmed at `scoring.md:9,15,26,38`). The shared vocabulary implies these are the same thing; they are not. | Principal | `references/heuristics.md` | Reword the heuristic bullet to say "pass_rate" instead of "score," or add a clause explicitly distinguishing it from `scoring.md`'s 0-100 score. |
| 17 | `references/local-models.md`'s Cross-links section (writer-confirmed: lists `ralph-loop.md`, `memory-patterns.md`, `artifacts.md`, `stall-recovery.md`, `docs/operator/running-the-loop.md`) has no pointer to/from `references/subagents.md`'s "Model selection" section, despite both discussing the same frugal/main model-selection flag pair — an asymmetric missing cross-link, not a contradiction. | Principal | `references/local-models.md`, `references/subagents.md` | Add `references/subagents.md` (Model selection) to `local-models.md`'s Cross-links, and add a pointer back from `subagents.md` to `local-models.md`. |

### Operator actions
> These need a human decision, external confirmation, or access the agent doesn't have.

| # | Finding | Raised by | Doc(s) | Why it needs a human |
|---|---|---|---|---|
| 1 | Beyond the mechanical doc-accuracy fix in Agent action #4: should wgm actually **build** real per-source filtering for the `sources:` field in `harvest-hive.sh`/`swarm.sh`, so trimming it has a real effect instead of being permanently disclosed as inert? | Senior (implicit in "...or implement real per-source filtering"), corroborated by PM's independent verification that the current honest-disclosure state is otherwise accurate | `scripts/harvest-hive.sh`, `scripts/swarm.sh`, `assets/wgm-hive.template.yml` | This changes real script behavior materially (what "one source" means as a filtering boundary, how it interacts with anonymization and de-dup) — a feature-scope commitment, not a bug fix. An agent can (and should, per Agent action #4) make the docs stop contradicting each other today without this decision being made first. |
| 2 | Should the ADR's three implicit constraints ("stay portable/lean," "prefer the simplest elegant solution," minimize steps / "rule of 3") be formalized as a **permanent, binding Ground Rule** in `CONTRIBUTING.md`, applying to all future contributions — not just this one ADR's local reasoning? | Principal | `CONTRIBUTING.md`, `docs/plans/2026-07-08_SKILLOPT_ADOPTION.md` | Principal already drafted candidate wording ("prefer zero new runtime dependencies; keep changes to the smallest number of files/commands"), so the text itself isn't the blocker — but `CONTRIBUTING.md`'s "Ground rules" is a small, deliberately curated, binding list for every future contributor. Elevating a one-off "session preference" into permanent project policy is a governance decision a maintainer should confirm, not something the agent should unilaterally enshrine. |
| 3 | What is the right **structural enforcement mechanism** for the docs-audit batching cadence, given PR #64 and PR #65 both silently skipped the mandatory checklist line for ~2.5 days with no recorded justification — a real breach of `references/docs-audit.md`'s own "batching must be explicit, not silently practiced" rule, caught only because this consolidated pass happened to run now? | PM | `docs/audit/README.md`, `.github/PULL_REQUEST_TEMPLATE.md` | The immediate coverage gap (PRs #62-#65 unaudited) is **already closed by this report's own filing** — that half of PM's recommended action needs no further work. What remains open is a genuine design choice: a new "audit debt: N PRs / since `YYYY-MM-DD`" line in `docs/audit/README.md`'s index (PM's suggestion), a CI check, or relying on this consolidated-report discipline alone going forward — deciding which one is a process-design call for a maintainer, not a mechanical fix. |

### Confirmed clean (no action)
> Findings the personas actively verified and found accurate — recorded so the paper trail shows
> what was checked and passed, not just what needs fixing.

- **`scripts/grade-evals.sh` / `scripts/test-grade-evals.sh` / `references/evals.md` /
  `CONTRIBUTING.md` / `SKILL.md` / `README.md`'s maturity and CI-wiring claims** — verified live,
  independently, three times over: Senior (7/7 pass), PM (7/7 pass, confirmed only
  `test-grade-evals.sh`, not `grade-evals.sh` itself, is wired into `Makefile`/CI), and the writer
  during this consolidation (re-ran `bash scripts/test-grade-evals.sh`, 7/7, matching
  `CONTRIBUTING.md`'s "(7 cases)" exactly — unlike the stale harvest-hive count above, this one is
  accurate). Also ties to PM's separate check that `docs/plans/2026-07-08_SKILLOPT_ADOPTION.md`'s
  `**Status:** accepted` matches these claims word-for-word.
- **`references/heuristics.md`'s PR #63 CI-vs-deploy-pipeline entry** — traced to `[learn]` issue
  #60; writer re-verified via `gh issue view 60`: state `CLOSED`, title matches the heuristic's "Why"
  field exactly. Both "Landed in" cross-references present. (PM)
- **Mandatory sweep** (`docs/plans/2026-06-16_RALPH_LANDSCAPE.md`'s point-in-time snapshot currency +
  `references/heuristics.md`'s Provenance ledger) — SkillOpt is properly logged in `heuristics.md`;
  no other file this round introduces an un-logged new external-tool citation. (Principal, PM)
- **Open-issue state and subagent count** — writer re-verified: `gh issue list --repo agent-frontier/wgm --state open`
  returns zero; `ls .github/agents/ | wc -l` → 12, matching every "twelve role-specialized subagents"
  claim checked this round. (PM)
- **`docs/audit/README.md`'s own index, prior to this report** — confirmed current: last entry
  `2026-07-06T0602Z` / PR #61 / AMBER; no doc anywhere falsely claims PRs #62-#65 were already
  audited. (PM) — see also the README index check section above.

### Dissent
> Disagreements between personas, preserved rather than averaged away.

| Finding | Persona A view | Persona B view | Preserved as |
|---|---|---|---|
| `references/local-models.md` missing from `README.md`'s `references/` repository-layout tree-comment (L268) | Principal: **RED** — this is a documented **third occurrence** of a defect class this project's own paper trail already rated RED once (`docs/audit/2026-07-05T0622Z_pr41-45-post-merge-audit.md` finding #8) and partially-fixed-then-regressed once more (PR #61); two dedicated fix-and-declare-done cycles have not held, so the recurrence itself — not just this one stale line — is the blocker. | Junior + Senior: **AMBER** — same underlying fact, filed as a routine completeness/consistency gap alongside other stale-enumeration nits, not a blocker. | Agent actions #2 and #3 — all three personas want the identical remedy (add the filename; Principal additionally wants a mechanical guard against a 4th recurrence), so the dissent is purely about severity/framing, not about what to do. Kept here rather than silently resolved because Principal's regression-history framing assigns a fundamentally different risk level to the same observable fact than Junior/Senior's routine-gap framing — and per `references/docs-audit.md`'s own "worst severity found" definition of the index's verdict field, Principal's minority RED reading survives into this report's Overall verdict even though it is the minority view among the three personas who raised this specific line. |

## Verdict history
- Previous entry: [2026-07-06T0602Z — PR #61 — AMBER](2026-07-06T0602Z_hive-growth-loop-pr61.md).
- **This run (2026-07-09T0113Z, PRs #62-#65): RED — regressed from AMBER.** PR #61's AMBER rested on
  most RED findings being verified fixed *before* that report was filed, leaving only
  Operator-deferred residue. This run has no such mitigation: it is a report-only pass (nothing fixed
  in this run), and it surfaces two independent RED-severity signals live in the current tree — a
  verified factual self-contradiction (`self-improvement.md` vs `wgm-hive.template.yml` on
  `sources:`) and a dissent-escalated third-time regression of an already-once-RED defect class
  (the `README.md` tree-comment gap). Per `references/docs-audit.md`'s own definition of the index's
  verdict field ("worst severity found") and the dissent-preservation discipline (a minority-flagged
  severity is preserved, not smoothed into the majority AMBER/GREEN), the overall verdict is RED
  rather than AMBER, even though roughly 20 of the 23 deduped findings this round are AMBER or GREEN
  and every one of them has a small, mechanical, already-specified remedy.
- This report itself **closes** the coverage half of PM's cadence-gap finding (Operator action #3) —
  PRs #62-#65 are no longer unaudited as of this filing. What remains open is only the enforcement-
  mechanism question (should a structural "audit debt" tracker replace reliance on PR-body checklists
  going forward), not the coverage gap itself.
- **Next required step:** once this file is saved to `docs/audit/2026-07-09T0113Z_pr62-65-post-merge-audit.md`,
  add its row to `docs/audit/README.md`'s newest-first index: `| 2026-07-09T0113Z | RED | PRs #62-#65 | [2026-07-09T0113Z_pr62-65-post-merge-audit.md](2026-07-09T0113Z_pr62-65-post-merge-audit.md) |` — keeping the README index check's own finding (above) from becoming stale the moment this report lands.
