# Docs Audit Report — 2026-07-05T0622Z — pr41-45-post-merge-audit

> Generated by the wgm docs-audit swarm (`wgm-docs-junior`, `wgm-docs-senior`, `wgm-docs-principal`,
> `wgm-docs-pm` → consolidated by `wgm-docs-writer`). This is the qualitative pass; structural checks
> (required files, links, fences, placeholders) live separately in `scripts/check-docs.sh` (GREEN at
> time of writing — 52 files, 33 mermaid diagrams, 11 agent files).

- **Scope:** full repo, consolidating four persona reviews (junior/senior/principal/PM) that ran
  independently against **PR #43** ("assimilate 3 GitHub agent-skill findings" — the
  `references/self-improvement.md` Cross-pollinate channel, `references/evals.md` /
  `evals/evals.json` / `scripts/check-evals.sh`, and two new `references/heuristics.md` provenance
  entries), then re-scoped at consolidation time because `main` had advanced two further merges past
  #43 before this pass could run. **Covers PRs #41-#45**, all unaudited until now (the last dedicated
  report, [`2026-07-04T2334Z_post-merge-refresh.md`](2026-07-04T2334Z_post-merge-refresh.md), covered
  only #38-#40):
  - **#41** `docs(audit): fresh post-merge docs-audit pass + fix a real gap found` — that report
    itself.
  - **#42** `docs(trigger-eval): add a skill-behaviour should/should-not fixture` —
    `references/trigger-eval.md`.
  - **#43** `feat(self-improvement): assimilate 3 GitHub agent-skill findings` — the PR the four
    persona reviews below were conducted against.
  - **#44** `feat(references): assimilate BMAD review + eval automation findings`.
  - **#45** `docs: assimilate Spec Kit and GSD grill/plan heuristics`.

  Before fixing anything, every finding was re-checked against the current (post-#44/#45) file
  state: #44 and #45 only added new assimilated content (they never touched the specific
  lines/sections these findings reference), so every finding's substance below is confirmed still
  valid, not incidentally already fixed.
- **Track:** Standard — a deferred, batched consolidation pass across five same-session
  Standard-track PRs, not a new feature build. This run is also the **first live application** of
  the batching allowance it itself lands in `references/docs-audit.md` (see Agent action #16) — a
  consolidated pass that explicitly names the PR range it covers, exactly as that new policy
  describes.
- **Trigger:** operator-requested consolidation of four already-completed persona reviews, re-scoped
  to close the #41-#45 coverage gap in one pass rather than five individual ones.
- **Overall verdict:** GREEN — 18 findings across all four personas (2 RED, 16 AMBER, two of the
  16 marked "soft"/"minor" by the reporting persona) were found; every one was fixed as an Agent
  action within this run. Zero Operator actions remain and zero dissent was recorded.
- **Unanimous:** no dissent recorded. All four personas agreed on every finding below — this is the
  first live application of `references/docs-audit.md`'s new unanimous-consensus rule (landed in
  PR #44, adapted from `BMAD-METHOD`'s Anti-Consensus Club).

## Persona findings

### Junior developer — clarity & onboarding
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `scripts/check-evals.sh` + `CONTRIBUTING.md` | `jq` is a brand-new hard dependency (only script in the repo using it), undocumented in `CONTRIBUTING.md`'s prerequisites, `README.md`, or `copilot-setup-steps.yml`. Worse: when `jq` is missing, the script does not report a missing-dependency error — it falls through to `FAIL: evals/evals.json is not valid JSON`, factually wrong and misleading. Only "works" in CI by luck (ubuntu-latest ships jq). | RED | Add `jq` to `CONTRIBUTING.md`'s dev-prerequisites; add a `command -v jq` guard at the top of `scripts/check-evals.sh` so the real cause surfaces. |
| `references/evals.md` (opening) | Claims it "closes the gap `references/trigger-eval.md` explicitly flagged" — but `trigger-eval.md` never states that gap itself; the "trigger + lifecycle tests" framing lives in `docs/plans/2026-06-16_RALPH_LANDSCAPE.md`, which `trigger-eval.md` cites as its own origin. A two-hop attribution presented as direct. | AMBER | Reword to credit the RALPH_LANDSCAPE watchlist directly: `trigger-eval.md` shipped the trigger half, this is the lifecycle half. |
| `references/heuristics.md` (evals-fixture "Backpressure" entry) | Doesn't restate the "schema-only" caveat every other mention of `check-evals.sh` in the repo carries — read standalone it could imply the script performs grading. | AMBER | Add a short parenthetical clarifying schema-only scope. |
| `SKILL.md` (References) · `docs/README.md` (Map) | Both `self-improvement.md` one-liners still describe only the old flywheel ("harvest lessons, report them upstream, promote durable ones") — neither mentions the new "Cross-pollinate" external-research channel. | AMBER | Extend both one-liners to mention harvesting internal lessons *and* cross-pollinating external research. |
| `references/artifacts.md` | Documents placement for every other `assets/*` template but not the newer `evals.template.json` (mirrors a pre-existing gap for `trigger-eval.md` too). | AMBER (soft) | Add a one-line cross-reference pointing to `references/evals.md` for the evals template's placement rule. |
| `references/ralph-loop.md`, `references/self-improvement.md`, `references/heuristics.md` | `saitarrun/devforge-ai` is cited 3× across the repo and never hyperlinked, unlike every other external citation (including ones added in PR #44/#45). | AMBER (minor) | Add a hyperlink on its fullest mention (`references/ralph-loop.md`'s Backpressure-in-depth bullet). |

### Senior developer — correctness, completeness, maintainability
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `scripts/check-evals.sh`, Makefile/CONTRIBUTING.md/CI wiring, all cross-links, `heuristics.md` entry format, external citations (mattpocock/skills, saitarrun/devforge-ai, agentskills/agentskills) | Verified: `check-evals.sh` behavior matches docs exactly (6/6 fuzz-tested failure modes correct); Makefile/CONTRIBUTING.md/CI wiring consistent; all cross-links resolve; `heuristics.md` entry format matches exemplars; every external citation verified byte-accurate, not fabricated. | GREEN | None — noted as a strength. |
| `references/self-improvement.md` (Cross-links) | The Cross-links footer was never updated to include the new `references/evals.md`, even though the "Cross-pollinate" section discusses it 3 times and `evals.md` links back. | AMBER | Add an `evals.md` entry to the Cross-links line. |
| `README.md` (Repository layout) | The tree-comment enumeration of `references/` and `assets/` contents omits several files added over the last few PRs: `trigger-eval`, `evals`/`evals.template.json`, `PLUGIN_PROTOCOL`, `plugin-integration`, `plugin-template`, `wgm.example.yml` (multi-PR-old debt, not unique to #43). | AMBER | Refresh both enumeration lines to include all of these. |
| `references/heuristics.md` (2 new PR #43 entries), `references/self-improvement.md` (Cross-pollinate step 3) | The two new entries use a `**Provenance:** external research, OWNER/REPO's THING` -style prefix, but `self-improvement.md`'s own instruction ("cite exactly as existing entries already do... e.g. `ghuntley/Ralph standing guardrail`") doesn't itself use that prefix — the PR's entries don't literally follow the instruction it just wrote. PR #44/#45 have since added several more entries using this same prefix (9 total now), making it clearly the established convention. | AMBER | Amend Cross-pollinate step 3 to explicitly bless the `external research, OWNER/REPO's THING` form as the expected prefix for this provenance category, rather than removing the prefix from now-multiple entries. |
| `references/evals.md` (opening) | The opening sentence ("closes the gap... wgm had no way to mechanically self-test end-to-end") is in tension with its own later section ("neither is a script wgm runs on itself... only their shape is checked mechanically today"). | AMBER | Soften the opening — combine with the Junior finding above into one clean rewrite. |

### Principal developer — architecture, strategic fit, conformance
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| `references/self-improvement.md` ("Cross-pollinate"), `references/gene-transfusion.md` | Verified: the Cross-pollinate section successfully avoids confusion with `gene-transfusion.md` via an explicit disambiguation sentence; no other file conflates them. | GREEN | None — noted as a strength. |
| `references/heuristics.md` | Verified: the ledger convention (one-thought-per-entry, newest-at-top, format) is fully respected across all new entries. | GREEN | None — noted as a strength. |
| `SKILL.md` | Reported as 292 lines pre-PR#44/45 (budget ~500); re-verified current line count and confirmed still comfortably under budget after #44/#45's additions. | GREEN | None — confirmed: #44 didn't touch `SKILL.md`, #45's edit was 1 line net-neutral, so it was still 292 lines pre-this-run; 296 after this run's own edits (see Consolidated report). |
| `references/self-improvement.md` (Cross-pollinate step 3) | Licenses skipping the `[learn]`-issue round-trip based on a single condition ("because this is discovered while already working **in** `agent-frontier/wgm`"), but wgm repeatedly dogfoods **itself** — a future *internal* lesson discovered mid-session in this same repo could misread this sentence as licensing the same skip. | AMBER | Tighten to a conjunctive condition: external research *and* already working in `agent-frontier/wgm` (not an internal dogfood lesson). |
| `references/self-improvement.md` (Report section / Cross-pollinate) | The "one lesson per report, maps to a single PR" discipline is silent on whether Cross-pollinate findings can be batched — yet PR #43 bundled 3 findings into one PR, and PRs #44/#45 (same research session) each bundled 4-7 findings into one PR too, reinforcing this is the actual, repeated, intentional practice. | AMBER | Add an explicit sentence/step stating a single research session's findings may be batched into one or a few PRs. |
| `references/trigger-eval.md`, `references/evals.md`, `evals/evals.json` | Two of the five seed `evals/evals.json` cases (`trivial-edit-no-trigger`, `mode-vs-request-disambiguation`) use the same underlying prompts as `trigger-eval.md` rows 13 and 5 (`evals.md` admits "mirrors row N"), but neither file's maintenance instructions say a mode-parsing edit must also re-check the mirrored case — a silent semantic-divergence risk neither check script would catch. | AMBER | Add a cross-reference in `trigger-eval.md`'s "How to use this" instructing that a mode-parsing edit must also re-check any evals.json case tagged "mirrors row N." |
| `README.md` ("Capabilities"), `docs/README.md` ("Map") | Neither indexes `references/evals.md` or `references/trigger-eval.md` — both exist only inside `SKILL.md`'s own References section, an indexing gap `references/docs-audit.md` itself names as exactly the kind of thing this audit should catch. | AMBER | Add one bullet/row each for `trigger-eval.md` and `evals.md`. |
| `SKILL.md` (References, `assets/` bullet) | Lists `evals` in the same parenthetical as the nine templates Phase 2 (Plan) actually scaffolds per-build, but `evals` is categorically different (wgm's own self-test fixture, not something scaffolded into arbitrary builds). | AMBER | Move `evals` out of the shared parenthetical into its own clause with a qualifier. |

### Project manager — status, risk, traceability
| Doc | Observation | Severity | Recommended action |
|---|---|---|---|
| GitHub state | Verified: 0 open PRs/issues at time of review (45 PRs, all merged; 9 `[learn]` issues #29-#37, all closed); numeric claims (51 docs files, 33 mermaid diagrams, 5 eval cases) all accurate. | GREEN | None — noted as a strength. |
| Remote branches | Branch-cleanup finding is now moot/resolved — re-verified live via `git ls-remote --heads origin`: only `main` exists on the remote; all feature branches already cleaned up. | GREEN (resolved) | None. |
| `SKILL.md` Phase 4, `references/docs-audit.md` | Both say the docs-audit is "**mandatory... Ship is not complete without this report**" / "**blocks Ship** the same way a failing test does" — but actual practice across PRs #39, #40, #42, #43 (and now #44, #45 too) has been to ship Standard-track PRs without individual audits and defer to periodic consolidated passes (like this one), with this batching cadence never itself written down as sanctioned policy anywhere. | RED (standout) | Amend `docs-audit.md`'s "When it runs" to explicitly sanction batching (option b — retroactively auditing every individual PR is not practical, and the batching pattern is clearly the real, repeated, working practice); soften `SKILL.md` Phase 4's bullet correspondingly without weakening the requirement that a consolidated pass must eventually happen. |
| `docs/audit/README.md` | The index table has no "covers up to / PR range" column, so a reader can't see audit-coverage lag without opening each report's prose "Scope:" line. | AMBER | Add a "Covers" column showing the PR range each report covers. |
| `evals/evals.json` | Zero in-file pointer back to `references/evals.md` explaining its purpose — contrast `assets/evals.template.json`, which has a self-deleting `_instructions` key. | AMBER | Add either a short `evals/README.md` or a non-schema `"_see"` top-level key (verified: `scripts/check-evals.sh` only checks required-key presence, so an extra key doesn't break it). |

## Consolidated report (wgm-docs-writer)

### README index check
Found and fixed during this run: root `README.md`'s Capabilities list and `docs/README.md`'s Map
table did not index `references/evals.md` or `references/trigger-eval.md` (Principal finding #14) —
both now indexed in both files. Also refreshed `README.md`'s Repository-layout tree-comment
enumeration for `references/` and `assets/`, which had fallen behind several PRs of debt (Senior
finding #8: missing `trigger-eval`, `evals`, `PLUGIN_PROTOCOL`, `plugin-integration`,
`plugin-template`, `wgm.example.yml`). No other orphaned links or unindexed files found in this
scope; `scripts/check-docs.sh` check 4 (link resolution) confirms green.

### Agent actions
> The agent can execute these directly and deterministically — no human judgment required.

| # | Finding | Raised by | Doc(s) | Action |
|---|---|---|---|---|
| 1 | `jq` undocumented hard dependency; missing-`jq` case prints a misleading JSON error | Junior | `scripts/check-evals.sh`, `CONTRIBUTING.md` | **Done in this run** — added a `command -v jq` guard (exit 2, distinct from schema-failure exit 1) that prints "jq is required..."; added `jq` to `CONTRIBUTING.md`'s Dev prerequisites table. Manually re-verified with jq hidden from `PATH` (see validation output). |
| 2 | `evals.md`'s opening mis-attributes a two-hop citation as direct | Junior | `references/evals.md` | **Done in this run** — reworded to credit `docs/plans/2026-06-16_RALPH_LANDSCAPE.md`'s watchlist directly; `trigger-eval.md` named as the trigger half, this file as the lifecycle half. Combined with row 10's fix (see below) into one rewrite of the same paragraph, per the task's own suggestion. |
| 3 | `heuristics.md`'s evals-fixture entry omits the schema-only caveat | Junior | `references/heuristics.md` | **Done in this run** — added "(`scripts/check-evals.sh` checks the fixture's shape only; grading stays manual/LLM-judged)" to the Backpressure-section entry. |
| 4 | `self-improvement.md` one-liners (SKILL.md + docs/README.md) don't mention Cross-pollinate | Junior | `SKILL.md`, `docs/README.md` | **Done in this run** — both one-liners now read "harvest internal lessons and cross-pollinate from external research, report them upstream, and promote durable ones" (SKILL.md) / "harvest internal lessons + cross-pollinate external research → report upstream → promote durable lessons" (docs/README.md Map). |
| 5 | `artifacts.md` has no placement rule for `evals.template.json` | Junior | `references/artifacts.md` | **Done in this run** — added a new `## evals/evals.json` section cross-referencing `references/evals.md` for the full discipline, distinguishing it from the per-build templates above it. |
| 6 | `saitarrun/devforge-ai` cited 3× but never hyperlinked | Junior | `references/ralph-loop.md` | **Done in this run** — hyperlinked on its fullest mention (Backpressure-in-depth's spec-drift pre-check bullet): `[`saitarrun/devforge-ai`](https://github.com/saitarrun/devforge-ai)`. |
| 7 | `self-improvement.md`'s Cross-links footer omits `evals.md` | Senior | `references/self-improvement.md` | **Done in this run** — added an `evals.md` entry (a Cross-pollinate-landed capability) to the Cross-links line. |
| 8 | `README.md` Repository-layout tree comments omit several recent files | Senior | `README.md` | **Done in this run** — refreshed both the `references/` line (added `trigger-eval`, `evals`, `PLUGIN_PROTOCOL`, `plugin-integration`) and the `assets/` line (added `evals`, `plugin-template`, `wgm.example.yml`). |
| 9 | New `heuristics.md` entries use an unblessed `external research, ...` provenance prefix | Senior | `references/self-improvement.md` | **Done in this run** — Cross-pollinate step 3 now explicitly blesses the `external research, OWNER/REPO's THING` form as the expected prefix for this category, distinguishing it from a bare design-lineage citation (e.g. "ghuntley/Ralph standing guardrail") or an internal `wgm dogfood, [learn] issue #N` entry. Confirmed 9 heuristics.md entries now use this prefix (2 from PR #43, 4 from #44, 3 from #45) — option (b), not removal, was correct. |
| 10 | `evals.md`'s opening overclaims relative to its own later section | Senior | `references/evals.md` | **Done in this run** — same rewrite as row 2: now reads "the *fixture format* now exists; running and grading it end-to-end still requires a maintainer or a future automated judge (see... below)." |
| 11 | Cross-pollinate's skip-condition isn't conjunctive, risks misapplication to internal lessons | Principal | `references/self-improvement.md` | **Done in this run** — step 3 now requires *both* "found via external research" *and* "already working in `agent-frontier/wgm`," explicit that an internal lesson discovered mid-session does not qualify for the shortcut. |
| 12 | Silent on whether Cross-pollinate findings may be batched into one PR | Principal | `references/self-improvement.md` | **Done in this run** — added new step 5 explicitly permitting a single research session's findings to be batched into one or a few PRs; updated the "Worked example" paragraph to note its 3 findings were batched this way. |
| 13 | No cross-reference tying mode-parsing edits to mirrored `evals.json` cases | Principal | `references/trigger-eval.md` | **Done in this run** — added a bullet to "How to use this" naming the two mirrored cases (`trivial-edit-no-trigger` ↔ row 13, `mode-vs-request-disambiguation` ↔ row 5) and noting neither check script catches cross-file divergence. |
| 14 | `evals.md`/`trigger-eval.md` unindexed in README.md/docs/README.md | Principal | `README.md`, `docs/README.md` | **Done in this run** — added one Capabilities bullet each to `README.md` and one Map row each to `docs/README.md` for both files. |
| 15 | SKILL.md bundles `evals` into the 9-template scaffolding parenthetical | Principal | `SKILL.md` | **Done in this run** — `evals.template.json` moved into its own clause: "wgm's own self-test fixture skeleton — not scaffolded into arbitrary builds; see `references/evals.md`." |
| 16 | Docs-audit "mandatory per-PR" policy text doesn't match actual batching practice | PM (standout) | `references/docs-audit.md`, `SKILL.md` | **Done in this run** — added a "Batching across a same-session run of PRs" paragraph to `docs-audit.md`'s "When it runs," explicitly sanctioning deferral across same-session Standard-track PRs provided a consolidated pass runs before the session ends (or a ~3-5 PR cap, matching the loop's own open-PR ceiling), whichever comes first — cross-referencing `docs/audit/README.md`'s new "Covers" column (row 17) as the mechanism. Softened `SKILL.md` Phase 4's bullet to match, without weakening the eventual-consolidated-pass requirement. **This report is itself the first worked example** of the policy it amends — a single dedicated pass explicitly covering PRs #41-#45. |
| 17 | `docs/audit/README.md` index has no PR-range column | PM | `docs/audit/README.md` | **Done in this run** — added a "Covers" column; backfilled it for both prior entries (`#38` for 1912Z, `#38-#40` for 2334Z) and this entry (`#41-#45`). |
| 18 | `evals/evals.json` has no in-file pointer back to its docs | PM | `evals/evals.json` | **Done in this run** — added a top-level `"_see": "references/evals.md"` key. Re-ran `scripts/check-evals.sh` after adding it: still GREEN, confirming the extra key doesn't break the schema check. |

### Operator actions
> These need a human decision, external confirmation, or access the agent doesn't have.

None this run — every finding above was directly and deterministically fixable, and all 18 were
fixed as Agent actions.

### Dissent
> Disagreements between personas, preserved rather than averaged away.

**Unanimous: no dissent recorded.** All four personas agreed on every finding in scope — no persona
contradicted another's severity or recommended action anywhere in this pass. Per
`references/docs-audit.md`'s new rule (landed in PR #44, adapted from `BMAD-METHOD`'s
Anti-Consensus Club), this is stated explicitly rather than left implicit: unanimous agreement is a
data point, not just an absence of conflict. This is that rule's first live application.

## Verdict history
- [2026-07-04T1912Z](2026-07-04T1912Z_docs-audit-core.md) — AMBER (one defect found+fixed, one
  dissent preserved, one confirmed-no-action).
- [2026-07-04T2334Z](2026-07-04T2334Z_post-merge-refresh.md) — GREEN, improved from AMBER: the prior
  dissent and confirmed items needed no further action; one new minor gap found and fixed in this
  same run.
- This entry (2026-07-05T0622Z) — GREEN, held from GREEN: 18 findings across PRs #41-#45 (2 RED, 16
  AMBER) were found by the four-persona swarm and every one fixed as an Agent action in this same
  run; zero Operator actions; zero dissent (first use of the unanimous-consensus rule). The
  standout RED (PM finding #16) turned a previously undocumented practice — batching the docs-audit
  across several same-session Standard-track PRs — into explicit, sanctioned policy, with this
  report itself serving as the first worked example of that policy.
